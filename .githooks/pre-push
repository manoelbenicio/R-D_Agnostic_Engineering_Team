#!/bin/sh
set -eu
[ "$#" -ge 2 ] || { echo 'pre-push remote name or location unavailable' >&2; exit 2; }
ROOT=$(git rev-parse --show-toplevel)
exec python3 "$ROOT/scripts/ops/git-object-size-gate.py" --pre-push --remote-name "$1" --remote-location "$2"
