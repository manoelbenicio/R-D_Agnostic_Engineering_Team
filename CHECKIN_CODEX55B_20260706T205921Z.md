# CHECKIN CODEX55B 20260706T205921Z

## Escopo
- Fatia: isolamento de credencial para GLM/Cline/OpenCode no `execenv`.
- Arquivos alterados por esta fatia:
  - `multica-auth-work/server/internal/daemon/execenv/execenv.go`
  - `multica-auth-work/server/internal/daemon/execenv/cline_home.go`
  - `multica-auth-work/server/internal/daemon/execenv/cline_home_test.go`
  - `multica-auth-work/server/internal/daemon/execenv/opencode_home.go`
  - `multica-auth-work/server/internal/daemon/execenv/opencode_home_test.go`
  - `multica-auth-work/server/internal/daemon/execenv/vendor_credential_fallback_test.go`

## Implementado
- Cline:
  - Prepara `cline-data-dir` por tarefa.
  - Aceita conta materializada como `AccountHome/.cline` ou como data-dir direto.
  - Expõe env via `Environment.CredentialEnv("cline")`:
    - `CLINE_DATA_DIR`
    - `CLINE_SANDBOX=1`
    - `CLINE_SANDBOX_DATA_DIR`
- OpenCode:
  - Prepara `XDG_DATA_HOME` por tarefa com `opencode/auth.json`.
  - Prepara `XDG_CONFIG_HOME` por tarefa com `opencode/` config.
  - Aceita layout home-like (`.local/share/opencode`, `.config/opencode`) e layout XDG data root (`opencode/auth.json`).
- GLM:
  - Usa o mesmo isolamento OpenCode-compatible (`XDG_DATA_HOME` + `XDG_CONFIG_HOME`), conforme o mapeamento local de GLM via OpenCode/GLM fleet.
- Fallback:
  - Sem `CredentialAccountHome`, `kiro`, `antigravity`, `cline`, `opencode` e `glm` continuam sem homes isolados criados por `execenv`.

## Agent backend
- `server/pkg/agent` ja tinha backend OpenCode.
- Nao alterei `agent.go` para Cline/GLM nesta fatia:
  - Cline tem binario local e contrato CLI, mas nao ha backend/whitelist/migration `protocol_family` existentes para `cline`.
  - GLM aparece como modelo/vendor dentro do caminho OpenCode, sem backend Go de primeira classe.
  - Adicionar esses backends com seguranca exigiria coordenar `server/pkg/agent`, `internal/daemon/config.go`, `migrations/120_runtime_profile.up.sql`, model listing e parser de output. Isso cruza fronteiras alem do wiring de `execenv` e colidiria com mudancas simultaneas em `daemon.go`.

## Evidencia
- Formatacao:
  - `docker run --rm -v "$PWD":/work -w /work golang:1.26-alpine gofmt -w ...`
- Testes verdes da fatia:
  - `docker run --rm -v "$PWD/multica-auth-work/server":/work -w /work golang:1.26-alpine go test ./internal/daemon/execenv -run 'TestPrepare(Cline|OpenCode|GLM)|TestVendorCredentialFallback|TestPrepareKiroHome|TestPrepareAntigravityHome'`
  - Resultado: `ok github.com/multica-ai/multica/server/internal/daemon/execenv 0.035s`
- Agent package sanity:
  - `docker run --rm -v "$PWD/multica-auth-work/server":/work -w /work golang:1.26-alpine go test ./pkg/agent -run 'TestNewReturnsOpenCodeBackend|TestSupportedTypes'`
  - Resultado: `ok github.com/multica-ai/multica/server/pkg/agent 0.012s`
- Compile daemon:
  - `docker run --rm -v "$PWD/multica-auth-work/server":/work -w /work golang:1.26-alpine go test ./internal/daemon -run '^$'`
  - Resultado: `ok github.com/multica-ai/multica/server/internal/daemon 0.010s [no tests to run]`
- Diff hygiene:
  - `git diff --check -- <arquivos-da-fatia>`
  - Resultado: sem output.

## Observacoes
- Nao commitei.
- Nao toquei em `daemon.go` nem `execenv/codex_home.go`.
- `daemon.go`, `execenv/codex_home.go` e `.deploy-control/ASK_KIRO.md` ja aparecem modificados no worktree por outra frente; deixei intocados.
- A injecao final no subprocesso ainda depende de `daemon.go` consumir `Environment.CredentialEnv(...)` ou os novos campos `Cline*`/`OpenCode*`.
- A suite completa `go test ./internal/daemon/execenv` nao foi usada como gate desta fatia porque falha em teste Codex existente: `TestPrepareCodexHomeSeedsFromShared` espera symlink de `auth.json` e obteve arquivo regular. Esse arquivo e area Codex-A.
