# Check-in: GLM#52#B — RR-OBSERV

- agent: GLM#52#B
- stream: RR-OBSERV (Savings KPI + spend dimensions in dashboards-as-code + Savings formula doc)
- started_at: 2026-07-04T01:33:27Z
- status: IN_PROGRESS
- files_locked (NEW / isolated only):
  - scripts/observability/components.savings.yaml            (NEW)
  - deploy/observability/grafana/dashboards/savings.generated.json  (GENERATED)
  - docs/project/savings-kpi.md                              (NEW)

## Grounding
- design.md §8 read (Observabilidade + aba SAVINGS = KPI DO MOAT "$ economizado rotacionando assinaturas vs metered"). Savings KPI is NEW; invent no metric names.
- gen_dashboards.py read: validates panel.metric against CATALOG_METRICS, panel.type in {timeseries,stat,gauge,table}, query must reference metric name literally. Output file = `<component.name>.generated.json`; dashboard title = component.title; uid = component.name.
- credential_metrics.go read: real metrics = rotation_total{vendor,reason,result}, rotation_duration_seconds{vendor}, all_accounts_exhausted{vendor}, accounts_available{vendor}, account_status{vendor,account_id,status}, account_tokens_used{vendor,account_id}, account_window_seconds_remaining{vendor,account_id}, exhaustion_detected_total{vendor,signal}, credential_restore_total{vendor,result}, cred_env_injection_total{vendor,result}, credential_prepare_seconds{vendor}.
- Grafana container multica-grafana UP, API http://localhost:3000.
- Other IN_PROGRESS check-ins read: Codex#5.5#B (RR-FALLBACK, rotation Go), GLM#52#A (RR-REGISTRY, migrations/sql). No conflict with my NEW files.

## Notes
- Savings = ESTIMATED derived expression (observed tokens x hypothetical metered price); marked ESTIMATED in panel title. Gap documented in savings-kpi.md. No product Go / existing dashboards edited; no git commit.

finished_at: 2026-07-04T01:35:52Z
agent confirmed: GLM#52#B
status: DONE
build_result: |
  $ python3 scripts/observability/gen_dashboards.py scripts/observability/components.savings.yaml
  wrote deploy/observability/grafana/dashboards/savings.generated.json

  $ python3 -c "import json; json.load(open('.../savings.generated.json'))"
  json ok; title= Savings KPI (Moat) ; uid= savings ; panels= 7

  $ docker restart multica-grafana >/dev/null && sleep 6
  restarted multica-grafana; password read (MASKED: ****)

  $ curl -s -u admin:**** "http://localhost:3000/api/search?query=Savings"
  found: 1
   - Savings KPI (Moat) | uid= savings

deliverables:
  - scripts/observability/components.savings.yaml (NEW) — 7 panels, only real catalog metrics; Savings marked ESTIMATED.
  - deploy/observability/grafana/dashboards/savings.generated.json (GENERATED) — valid JSON, loaded by Grafana.
  - docs/project/savings-kpi.md (NEW) — formula, inputs, moat-in-$, gap (per-vendor metered price + model/context weighting + prompt/completion split not yet instrumented).
notes:
  - No metric fabricated: every panel references a real series from credential_metrics.go.
  - Savings estimate uses a documented in-query constant P_metered=3e-6 USD/token (gap flagged).
  - No product Go / existing dashboards edited; no git commit. Grafana password MASKED throughout.
