agent: GLM-52
stream: W-OBS
started_at: 2026-07-01T19:28:27Z
finished_at: 2026-07-01T19:47:14Z
status: DONE
files_locked:
  - deploy/observability/**
depends_on: []
build_result: >
  GREEN. Validacoes executadas em multica-auth-work/deploy/observability:
  - docker compose config >/dev/null -> COMPOSE_OK.
  - promtool check config prometheus.yml -> SUCCESS (1 rule file, 7 rules). NOTA:
    as imagens oficiais tem ENTRYPOINT=prometheus/alertmanager, entao o comando
    literal do prompt ("docker run prom/prometheus promtool ...") falha com
    "unexpected promtool". A forma correta e --entrypoint promtool (documentado
    no README); as CONFIGS em si passam 100% no promtool.
  - promtool check rules alerts.yml -> SUCCESS (7 rules).
  - amtool check-config alertmanager.yml -> SUCCESS (extra).
  - python3 -m json.tool nos 4 dashboards -> JSON_OK (todos validos).
  - PyYAML lint em todos os .yml (compose, prometheus, alerts, alertmanager,
    grafana provisioning) -> YAML_OK.
  - Wrapper do postgres-exporter viavel: /bin/sh e /bin/postgres_exporter presentes
    na imagem (roda como nobody); le docker secrets, monta DSN, exec binario.
  Nenhum .go tocado. Stream 100% independente do codigo Go.
notes: >
  Entregues em deploy/observability/: docker-compose.yml (prometheus 9090, grafana
  3000, alertmanager 9093, postgres-exporter 9187), prometheus.yml, alerts.yml
  (7 regras do doc 05 §4), alertmanager.yml (webhook local placeholder),
  grafana/provisioning (datasource Prometheus + dashboard provider) e
  grafana/dashboards (4 JSON: Credential Health, Accounts & Quota, Rotation,
  Platform Health), pg-exporter-entrypoint.sh, README.md, .env.example,
  secrets/*.example + arquivos placeholder (gitignored via .gitignore local).
  SEGREDOS: docker secrets — grafana via GF_SECURITY_ADMIN_PASSWORD__FILE (nativo);
  postgres-exporter via wrapper que le /run/secrets e monta DATA_SOURCE_NAME em
  runtime (a imagem nao tem *_FILE nativo). NENHUM segredo em
  metrica/label/config/log; apenas placeholders. Postgres-only (RNF-04/05).
  PLACEHOLDER: o scrape "credential-service" aponta para host.docker.internal:8081
  pois as metricas do doc 05 §3 (credential_restore_total, cred_env_injection_total,
  account_status, rotation_total, ...) ainda NAO estao instrumentadas no Go
  (streams W-VENDORS/W-INT). O backend do produto JA expoe /metrics via METRICS_ADDR
  (registry multica_*/go_*/process_*); o painel Platform Health usa essas metricas
  reais. Ajustar o target em prometheus.yml quando a instrumentacao Go publicar o
  /metrics do servico de credencial/daemon.
