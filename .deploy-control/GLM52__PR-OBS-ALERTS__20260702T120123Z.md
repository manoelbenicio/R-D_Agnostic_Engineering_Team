agent: GLM-5.2
stream: PR-OBS-ALERTS
started_at: 2026-07-02T12:01:23Z
finished_at: 2026-07-02T12:17:40Z
status: DONE
files_locked:
  - deploy/observability/alerts.yml            # EDIT (owned this stream) — real root: multica-auth-work/deploy/observability/alerts.yml
  - docs/project/observability-runbook.md      # NEW   — real root: multica-auth-work/docs/project/observability-runbook.md
do_not_edit:
  - product Go (*.go)
  - deploy/observability/prometheus.yml        # owned/managed by Opus
  - Grafana dashboards / grafana provisioning
depends_on: []
context_verified:
  stack_up:
    prometheus: localhost:9090 (HTTP 200 healthy, --web.enable-lifecycle on)
    alertmanager: localhost:9093 (HTTP 200 healthy)
    grafana: localhost:3000
    postgres-exporter: localhost:9187
    backend(credential-service): multica-backend-1 127.0.0.1:8080
  prometheus_targets: credential-service=up, postgres=up, prometheus=up
  prometheus_yml: rule_files=[alerts.yml]; jobs=credential-service(backend:9090),
    postgres(postgres-exporter:9187), prometheus(localhost:9090); alerts.yml mounted
    at /etc/prometheus/alerts.yml:ro. NOT edited (Opus-owned).
  alerts_yml_mount: ./alerts.yml:/etc/prometheus/alerts.yml:ro (docker-compose) ->
    editing host file + POST /-/reload reloads rules.
  real_metric_catalog (server/internal/metrics/credential_metrics.go — USE ONLY THESE):
    rotation_total{vendor,reason,result}, rotation_duration_seconds{vendor},
    all_accounts_exhausted{vendor}, accounts_available{vendor},
    exhaustion_detected_total{vendor,signal}, credential_restore_total{vendor,result},
    cred_env_injection_total{vendor,result}, credential_prepare_seconds{vendor},
    account_status{vendor,account_id,status}, account_tokens_used{vendor,account_id},
    account_window_seconds_remaining{vendor,account_id}
  forbidden_metric_removed: secret_in_log_suspected_total (in current alerts.yml line 58)
    is NOT a real metric -> alert removed. NO fake metrics added.
  daemon_metrics_gap: rotation runs in the DAEMON process which does NOT export a
    metrics server (BACKLOG-detection.md line 306: "expor metrics server no processo
    daemon"). So rotation_* / all_accounts_exhausted / accounts_available series are
    absent on backend /metrics today. Alerts are defined against the real names;
    live firing of rotation alerts awaits the daemon-metrics stream (H2). Proven
    firing uses a genuinely drivable signal (target-down / pg_up), NOT a faked
    rotation series.
  promtool: available in container (v3.13.0) for syntax check before reload.
build_result: |
  Files:
    - deploy/observability/alerts.yml  (EDIT, 122 lines) — rewritten: 10 rules across 3
      groups, REAL metric names only (credential_metrics.go + standard up/pg_up). Removed
      the fake `secret_in_log_suspected_total` alert (metric did not exist). Added the
      drivable target-down alerts (CredentialServiceDown, PostgresExporterDown) and the
      prompt-required AllAccountsExhausted / NoAccountsAvailable / RotationErrorsSpiking.
    - docs/project/observability-runbook.md (NEW, 267 lines) — stack bring-up, URLs/ports,
      every alert + meaning + first response, §6 daemon-metrics gap (H2) caveat, §7 what to
      watch, §8 verification cheat-sheet, §9 files touched.
  NOT touched: product Go, prometheus.yml (mtime preserved Jul 2 00:20), Grafana dashboards.
  NO fabricated metric used to force-fire anything.

  --- (1) promtool check rules (syntax valid, no parse errors) ---
  $ docker exec multica-prometheus promtool check rules /etc/prometheus/alerts.yml
  Checking /etc/prometheus/alerts.yml
    SUCCESS: 10 rules found
  PROMTOOL_EXIT=0

  --- (2) loaded rules (api/v1/rules after POST /-/reload -> HTTP 200) ---
  groups: 3
    credential-isolation -> ['CredentialRestoreFailing', 'EnvInjectionFailing', 'CredentialPrepareSlow']
    credential-rotation  -> ['AllAccountsExhausted', 'NoAccountsAvailable', 'RotationErrorsSpiking', 'ExhaustionDetectedSpiking']
    platform             -> ['PostgresDown', 'CredentialServiceDown', 'PostgresExporterDown']
  (10 rules total — matches promtool. No rule-parse errors; corruptionCount=0.)

  --- (3) alert state PROVEN on a real drivable signal (NO faked rotation metric) ---
  Rotation metrics are absent on backend /metrics today (verified: rotation_total=0 series,
  all_accounts_exhausted=0 series, accounts_available=0 series) because the daemon does not
  export a metrics server (H2 gap, BACKLOG-detection.md:306). So firing was proven with a
  genuine target-down signal — the SAME `up{job}==0 for 1m` mechanism as CredentialServiceDown
  — applied to PostgresExporterDown by cycling the pure-monitoring sidecar
  multica-postgres-exporter (zero product impact; backend/DB/daemon untouched).

  Baseline (all targets up):  prometheus=1  postgres=1  credential-service=1
  Drive:  docker stop multica-postgres-exporter  (T0 = 12:11:53Z)
  T+~60s FIRING WINDOW capture (12:13:16Z):
      up{job="postgres"} = 0
      alerts: 1
        PostgresExporterDown | state= pending | activeAt= 2026-07-02T12:12:16Z
  T+~130s RESOLVED WINDOW capture (12:14:03Z), after docker start at 12:13:16Z:
      up{job="postgres"} = 0  (scrape still catching up; eval lag)
      alerts: 1
        PostgresExporterDown | state= firing     <-- transitioned pending -> firing at the for:1m mark
  Post-resolution (current):  up postgres=1 ; PostgresExporterDown RESOLVED (gone);
      PostgresDown | state= firing   (pg_up==0 — the real, pre-existing exporter->DB condition,
      re-engaged once pg_up was scraped again after the exporter came back).

  Transition proven: PostgresExporterDown  inactive -> pending (12:12:16Z) -> firing (12:14:03Z)
  -> resolved (after restart). Driven by a REAL up{job="postgres"}==0 condition, not a faked
  rotation series. CredentialServiceDown (same up{job}==0 mechanism) is loaded and inactive
  (up{job="credential-service"}==1); not exercised by stopping the backend to avoid disrupting
  concurrent agents.

  --- (4) final live alert board ---
  alerts: 1
    PostgresDown | state= firing     (pg_up == 0 — real condition; documented in runbook §4.3/§7)

  --- (5) runbook verification ---
  docs/project/observability-runbook.md: 267 lines; 8 references to the daemon-metrics gap / H2;
  all 10 alert headings present (AllAccountsExhausted, NoAccountsAvailable, RotationErrorsSpiking,
  ExhaustionDetectedSpiking, CredentialRestoreFailing, EnvInjectionFailing, CredentialPrepareSlow,
  PostgresDown, CredentialServiceDown, PostgresExporterDown); §6 explicitly states live rotation
  alerts await the daemon-metrics stream (H2) and that rotation is proven via rotation_events
  (DB) today, with the exact psql queries.

  DONE: rules loaded (10, no parse errors) + one real alert transition proven
  (PostgresExporterDown pending->firing->resolved via driven up==0; PostgresDown firing via
  pg_up==0) + runbook written. Real metric names only.
notes: >
  Verification plan: (1) promtool check rules; (2) POST /-/reload; (3) /api/v1/rules
  shows all rules loaded with no parse errors; (4) /api/v1/alerts shows >=1 alert in
  pending/firing explained via a real drivable signal (CredentialServiceDown via
  target-down, or PostgresDown via pg_up==0) — NO fabricated rotation metric.
  Runbook covers stack bring-up, URLs/ports, every alert + first response, and the
  explicit H2 daemon-metrics caveat (rotation proven via rotation_events DB today,
  not Prometheus).
