# Smoke Suite — DRY-RUN Evidence

- executed_by: opus-4.8-orchestrator
- executed_at_utc: 2026-07-04T20:12:52Z
- mode: DRY-RUN (default; scripts print planned loopback request only; never start prodex / never deploy)
- IMPORTANT: LIVE-sidecar green remains GATED at F0 (deploy_owner_approved=false). This proves harness correctness + contract wiring, NOT a live pass.

## event-stream-smoke.sh
```
[event-stream-smoke] DRY-RUN: would GET http://127.0.0.1:43117/v1/events/stream?session_id=session-smoke
[event-stream-smoke] DRY-RUN: would validate NDJSON contract_version=rpp.l2.v1 and secrets_present=false
[exit_code: 0]
```

## kill-switch-smoke.sh
```
[kill-switch-smoke] DRY-RUN: would POST http://127.0.0.1:43117/v1/killswitch/apply feature=smart_context state=disabled effective_at=next_request
[exit_code: 0]
```

## policy-apply-smoke.sh
```
[policy-apply-smoke] DRY-RUN: would POST http://127.0.0.1:43117/v1/policy/apply
[policy-apply-smoke] DRY-RUN: policy uses Smart Context shadow, canary 0, auto-redeem disabled
[exit_code: 0]
```

## profile-fail-closed-smoke.sh
```
[profile-fail-closed-smoke] DRY-RUN: would POST http://127.0.0.1:43117/v1/accounts/register with invalid profile_home=/tmp/rpp-smoke-outside-managed-root
[profile-fail-closed-smoke] DRY-RUN: PASS condition is HTTP >= 400 or rejected_profiles non-empty
[exit_code: 0]
```

## readyz-smoke.sh
```
[readyz-smoke] DRY-RUN: would GET http://127.0.0.1:43117/readyz
[readyz-smoke] DRY-RUN: would validate contract_version=rpp.l2.v1, status=ready, checks all pass, shared_state_backend present
[exit_code: 0]
```

## redaction-smoke.sh
```
[redaction-smoke] DRY-RUN: would check logs dir: /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/logs
[redaction-smoke] DRY-RUN: would check events file: '<from /v1/events/stream>'
[redaction-smoke] DRY-RUN: would check evidence dir: /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/evidence
[redaction-smoke] DRY-RUN: would validate secret markers: sk-test-secret
Bearer=<redacted>
postgres://user:pass@example/db
redis://:pass@example:6379
[redaction-smoke] DRY-RUN: would assert secrets_present=false on event stream
[redaction-smoke] DRY-RUN: would reject events with secrets_present=true
[exit_code: 0]
```

## session-start-stop-smoke.sh
```
[session-start-stop-smoke] DRY-RUN: would POST http://127.0.0.1:43117/v1/session/start and validate router_owner=rust_l2
[session-start-stop-smoke] DRY-RUN: would POST http://127.0.0.1:43117/v1/session/stop with reason=operator_requested
[exit_code: 0]
```

## state-backend-smoke.sh
```
[state-backend-smoke] DRY-RUN: would GET http://127.0.0.1:43117/readyz
[state-backend-smoke] DRY-RUN: would validate contract_version=rpp.l2.v1, status=ready
[state-backend-smoke] DRY-RUN: would assert shared_state_backend check present and passing
[state-backend-smoke] DRY-RUN: would assert backend_type is postgres (not sqlite)
[exit_code: 0]
```

