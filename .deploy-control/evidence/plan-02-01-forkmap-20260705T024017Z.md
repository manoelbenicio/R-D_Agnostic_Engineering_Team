# PLAN 02-01 Forkmap Evidence

Status: DONE
Timestamp: 2026-07-05T02:40:17Z
Executor: Codex

## Scope

Requested plan path:
`.planning/phases/02-forkmap/02-01-PLAN.md`.

Local and fetched `origin/main` refs do not contain that exact file. The closest
authoritative phase document found was `Diligencias/02_FORKMAP_P2.md` in
`origin/main`, with the same fork-map objective and gate:

- map prodex workspace crates;
- isolate runtime proxy/gateway/Smart Context/state/redeem boundaries;
- preserve hard affinity, rotate-before-commit, and profile auth isolation;
- record fork gaps and evidence in `docs/prodex/prodex-fork-map.md`.

## Source Pin Verification

Pinned prodex source checked at:

- `/tmp/prodex-audit-7750da9`
- `/home/dataops-lab/runtime/prodex-src`

Both resolve to commit:

```text
7750da9b6a5c91a6d429e18e6a4d422cab4bc144
```

Both contain 44 crates under `crates/`.

## Verified Crate Inventory

```text
prodex-app
prodex-app-reports
prodex-audit-log
prodex-bench-support
prodex-caveman-assets
prodex-cli
prodex-codex-config
prodex-context
prodex-core
prodex-housekeeping
prodex-mcp-stdio
prodex-memory
prodex-presidio
prodex-profile-export
prodex-profile-identity
prodex-provider-core
prodex-proxy-config
prodex-quota
prodex-redaction
prodex-runtime-anthropic
prodex-runtime-broker
prodex-runtime-broker-log
prodex-runtime-capabilities
prodex-runtime-claude
prodex-runtime-cookies
prodex-runtime-doctor
prodex-runtime-gemini
prodex-runtime-gemini-cli-compat
prodex-runtime-launch
prodex-runtime-log
prodex-runtime-metrics
prodex-runtime-policy
prodex-runtime-proxy
prodex-runtime-quota
prodex-runtime-state
prodex-runtime-store
prodex-runtime-tuning
prodex-secret-store
prodex-session-store
prodex-shared-codex-fs
prodex-shared-types
prodex-state
prodex-terminal-ui
prodex-update-notice
```

## Fork-Map Gate Check

Artifact: `docs/prodex/prodex-fork-map.md`

Confirmed present:

- primary source ledger for prodex docs and code;
- runtime L2 crate map;
- runtime proxy, gateway, Smart Context, state, and redeem boundaries;
- explicit fork boundary: keep, wrap/change, and do-not-move-to-Go sections;
- non-blocking relationship to F0 prodex-as-is rollout.

Artifact: `docs/prodex/prodex-runtime-invariants.md`

Confirmed present:

- Go cold control plane and Rust/prodex hot runtime plane authority split;
- hard affinity for `previous_response_id`, `x-codex-turn-state`, and
  `session_id`;
- rotate-before-commit and no mid-stream rotation after model output starts;
- hot path I/O and persistence constraints;
- Smart Context exact fallback, shadow, and canary rules;
- gateway/provider, state backend, redeem, kill-switch, and secrets rules.

Artifact: `docs/prodex/prodex-gap-hardening-list.md`

Confirmed present:

- package pin/SBOM/attribution hardening;
- one runtime decision owner per session;
- sidecar health/readiness and policy/account registration gaps;
- kill-switch, shared state, provider conformance, Smart Context replay,
  redaction, diagnostics-only log, redeem, and rollback gates.

## Evidence Commands

Commands run without printing secrets:

```text
git fetch origin main --prune
git show origin/main:Diligencias/02_FORKMAP_P2.md
find /home/dataops-lab/runtime/prodex-src/crates -maxdepth 1 -mindepth 1 -type d
rg "previous_response_id|x-codex-turn-state|session_id|rotate|mid-stream|commit|PRODEX_HOME|profiles|auto-redeem|Smart Context|shadow|canary|gateway.state.backend|SQLite|Postgres|Redis" docs crates/...
```

## Verdict

PLAN 02-01 fork-map gate is satisfied by current docs and source audit.
No product code or deploy action was run.
