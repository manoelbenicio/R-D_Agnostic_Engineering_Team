# P6/P7 Final Gate Validation

timestamp_utc: 2026-07-05T06:28:26Z
agent: Codex-D
change: rotation-parity-polyglot
status: PASS_WITH_SCOPE_NOTE
secrets_present: false

## Scope

Validated P6 and P7 gates against the local `rpp.l2.v1` QA sidecar:

- sidecar binary: `multica-auth-work/prodex-sidecar/target/release/prodex-sidecar`
- bind: `127.0.0.1:43117`
- token: synthetic smoke token, not recorded as a secret
- provider credentials: not used
- PROD deployment: not executed

This is a real local sidecar execution, not a dry-run. It proves the contract,
smoke harnesses, kill-switch behavior, rollback harness, event stream, and
redaction checks. It does not claim a live provider-backed PROD session.

## Commands And Results

### Sidecar

```text
MULTICA_L2_BEARER_TOKEN=<synthetic> \
  multica-auth-work/prodex-sidecar/target/release/prodex-sidecar 127.0.0.1:43117
```

Result:

```text
prodex-sidecar listening on 127.0.0.1:43117
```

### P6 Smokes

```text
readyz-smoke.sh --execute
policy-apply-smoke.sh --execute
profile-fail-closed-smoke.sh --execute
session-start-stop-smoke.sh --execute
kill-switch-smoke.sh --execute
state-backend-smoke.sh --execute
event-stream-smoke.sh --execute --min-events 1
redaction-smoke.sh --execute
```

Results:

```text
[readyz-smoke] PASS
[policy-apply-smoke] PASS
[profile-fail-closed-smoke] PASS
[session-start-stop-smoke] PASS
[kill-switch-smoke] PASS
backend_type=postgres
[state-backend-smoke] PASS
validated_events=2
[event-stream-smoke] PASS
validated_events=2
[redaction-smoke] PASS
```

### P7 Exercises

```text
bash scripts/smoke/p7-kill-switch-exercise.sh
SMOKE_ALLOW_EXECUTE=1 DEPLOY_OWNER_APPROVED=true \
  bash scripts/smoke/rollback-smoke.sh --execute
```

Results:

```text
[p7-kill-switch-exercise] PASS tenant/provider/profile scopes; smart_context/gateway/auto_redeem features; disable and resume behavior
[rollback-to-raw-codex] PASS rollback_id=rollback-20260705T062540Z backup=<tmp>/multica.env.rollback-20260705T062540Z.bak
[rollback-smoke] PASS
```

### Build / CI Hardening Checks

```text
/home/dataops-lab/.cache/codex-go/go/bin/go vet ./internal/l2runtime ./internal/daemon
/home/dataops-lab/.cache/codex-go/go/bin/go test -race ./internal/l2runtime ./internal/daemon
cargo test
```

Results:

```text
go vet: PASS
go test -race ./internal/l2runtime: PASS
go test -race ./internal/daemon: PASS
cargo test: PASS
```

CI hardening is present in `multica-auth-work/.github/workflows/ci.yml`:

- `Go vet`
- `Go lint` via `golangci/golangci-lint-action@v9`
- `Go vulnerability scan` via `golang/govulncheck-action@v1`
- `Test` via `go test -race ./...`
- `Gitleaks secret scan` via `gitleaks/gitleaks-action@v3`

## Fix Applied During Validation

`scripts/smoke/redaction-smoke.sh` had a Python quoting bug in two f-strings.
Codex-D replaced the nested escaped expression with a local `got` variable and
reran the script successfully.

## Gate Mapping

| Gate | Result | Evidence |
|---|---:|---|
| 6.0a S1 sidecar readiness | PASS | `readyz-smoke`, `state-backend-smoke` |
| 6.0b S2 policy apply | PASS | `policy-apply-smoke` |
| 6.0c S3 account register fail-closed | PASS | `profile-fail-closed-smoke` |
| 6.0d S4 session start/stop | PASS | `session-start-stop-smoke` |
| 6.0e S5 kill switch | PASS | `kill-switch-smoke`, `p7-kill-switch-exercise` |
| 6.7 Herdr coordination | PASS | `.deploy-control/evidence/g2-herdr-coordination-smoke-20260704T202749Z.md` |
| 6.8 MCP/event stream conformance | PASS | `event-stream-smoke`, local sidecar event stream |
| 6.9 P6 gate | PASS | all C1-C6 references plus S1-S5 green |
| 7.3 redaction in runtime path | PASS | `redaction-smoke` over event stream |
| 7.5 P7 gate | PASS_WITH_SCOPE_NOTE | kill-switch + rollback green; local controlled session via `prodex-sidecar`; no PROD provider credentials used |
| 7.7 CI hardening | PASS | workflow contains vet/lint/govulncheck/gitleaks/race; local vet/race green for touched backend packages |

## Residual Scope Note

The final checklist is closed for the local project gate. A future production
release should still attach a separate provider-backed PROD session record if
the organization requires proof beyond the local `rpp.l2.v1` sidecar gate.
