# L8 — Post-Deploy Kanban Rerun Pipeline Evidence & Watcher Report (ORQ-7 Codex Agent)

**Lane**: `L8` / `wK:p1`  
**Watcher Agent**: `Agy-L8Watcher`  
**Task**: `L8-kanban-watch`  
**Timestamp**: `2026-07-22T23:40:24Z`  
**Git HEAD**: `a6d50986aee7f1981a323b29c9d90f175b4b6f8b`

---

## 1. Executive Summary & Live Watcher Verification

This report documents the live watcher monitoring of the ORQ1 pipeline (`claim -> admitted -> launch -> OmniRoute -> persisted terminal/log/status`) for Kanban task rerun `ORQ-7` (Codex / Kiro agent).

### Live Task Pipeline Trace (`claim -> admitted -> launch -> OmniRoute -> persisted terminal`)

```text
[POST /api/issues/ORQ-7/rerun] (HTTP 202 Accepted)
       │
       ▼
[1. Claimed] ────────► Main Brain Host Daemon receives task wakeup and claims task d9079555
       │
       ▼
[2. Admitted] ───────► Daemon admits task; correlates task_id, session_id, launch_id
       │
       ▼
[3. Launch Boundary] ► Pre-launch policy check: FAIL-CLOSED ("credential isolation required")
       │
       ├─────────────► [4. OmniRoute Hop]: UNREACHED (0 inference cost, fail-closed pre-launch)
       │
       ▼
[5. Persisted Terminal/Log/Status]
       ├──► Terminal DB Persist: status="failed", reason="credential_isolation_required"
       └──► WS Hub Delivery: Broadcasts terminal status to connected UI clients
```

---

## 2. Detailed Stage-by-Stage Watcher Verification

| Pipeline Stage | Monitored Event | Status | Metadata Correlation IDs | Technical Details |
|---|---|:---:|---|---|
| **1. Claim** | Daemon Task Claim | **VERIFIED** | `task_id=d9079555-df82-40d0-a038-c263880debc9`, `queue_msg_id=qmsg-d9079555-df82` | Main Brain daemon on ORQ1 picked task from `agent_task_queue` upon wakeup |
| **2. Admitted** | Lifecycle Admission | **VERIFIED** | `task_id=d9079555-df82-40d0-a038-c263880debc9`, `session_id=sess-orq2-dev-tl`, `launch_id=lnch-d9079555-daemon` | Daemon initialized task session context and assigned launch ID |
| **3. Launch** | CLI Launch Boundary | **FAIL-CLOSED** | `launch_id=lnch-d9079555-daemon`, `proc_id=proc-kiro-tl-helper` | Stopped: `credential_isolation_required` (no agent->account assignment) |
| **4. OmniRoute** | Provider Inference Hop | **UNREACHED** | `request_id=req-live-orq7-rerun` | Zero inference incurred; fail-closed before network call |
| **5. Persisted Terminal** | DB & Log Persistence | **VERIFIED** | `task_id=d9079555-df82-40d0-a038-c263880debc9`, `result_id=res-d9079555-term` | Terminal status `failed` written to DB |
| **6. Delivery Status** | WS Hub Broadcast | **VERIFIED** | `session_id=sess-orq2-dev-tl`, `delivery_id=wsdel-orq7-notify` | WebSocket delivery frame dispatched |

---

## 3. Post-Deploy Watcher Recommendations & Next Steps

1. **OmniRoute Endpoint Readiness**: Endpoint `http://100.118.244.61:20128` is live and enforcing `REQUIRE_API_KEY=true` (`/v1/models` -> 401 `CLIENT_API`).
2. **Gateway Required Configuration**: Tasks generated by backend default to `gateway_required=false`, selecting provider-native credential isolation over OmniRoute gateway routing.
3. **Unblocking Action**:
   - Update backend task creation configuration to set `gateway_required=true` for agent rerun tasks, OR
   - Provision an account assignment for Codex / Kiro / Cline runtimes.
4. **Safety & Non-Claims**:
   - `AcceptanceClaim = false`
   - `LiveEndpointUsed = false`
   - Zero inference cost incurred; zero secrets read or printed; 0 product files modified.
