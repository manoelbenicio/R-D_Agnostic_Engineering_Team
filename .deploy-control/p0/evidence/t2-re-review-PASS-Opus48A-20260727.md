[FROM Opus48#A][ORQ2 w6:p1][2026-07-27T10:26Z][POS-FIX RE-REVIEW T2] VEREDITO: PASS. Os dois achados que eu havia levantado estao fechados, com linha exata. 1) F1 FECHADO - fallback do sweeper migrado. cmd/server/runtime_sweeper.go:299 cria fallbackTaskSvc := &service.TaskService{Queries: queries, Bus: bus} e a :308 faz workspaceID := fallbackTaskSvc.ReconcileFailedIssue(ctx, t, false), com o comentario 306-307 declarando reuso da mesma guarda atomica. Confirmei a REMOCAO do caminho nao atomico: grep de HasActiveTaskForIssue e de UpdateIssueStatus em runtime_sweeper.go retorna ZERO ocorrencias, ou seja o check-then-act das antigas 313-314 e o UpdateIssueStatus sem guarda e sem tratamento de erro nao existem mais. Nao sobrou terceiro caminho de escrita de status nesse arquivo. 2) F2 FECHADO - issue:updated so em reset CASADO. internal/service/task.go:1888 ReconcileFailedIssue agora e exportada e decide por switch nas linhas 23-34 do corpo: case err == nil chama s.broadcastIssueUpdated(updated), case errors.Is(err, pgx.ErrNoRows) e no-op silencioso comentado como resultado esperado da guarda, e default apenas loga slog.Warn. Como a query e :one com WHERE i.status = 'in_progress' e NOT EXISTS, zero linhas devolve ErrNoRows, logo NAO ha evento em no-op. O evento carrega a issue JA atualizada, task.go:2219-2228 broadcastIssueUpdated publica protocol.EventIssueUpdated com WorkspaceID e payload issueToMap(issue, prefix), e a linha 25 passa updated, o retorno do RETURNING, nao a leitura previa. Exatamente o casamento pedido. 3) QUERY E GERADO - sem divergencia, reconfirmado. pkg/db/queries/issue.sql mantem @id, @workspace_id, i.status = 'in_progress' e NOT EXISTS com IN queued, dispatched, running, waiting_local_directory; pkg/db/generated/issue.sql.go:1247-1272 usa dolar 1 e dolar 2 na mesma ordem de ResetIssueToTodoIfNoActiveTaskParams e expande RETURNING i asterisco nas mesmas 24 colunas e mesma ordem da expansao pre-existente de updateIssueStatus, com row.Scan acompanhando. 4) TESTES - cobrem o que importa e travam regressao. internal/service/task_complete_race_test.go:265 TestReconcileFailedIssueUsesAtomicGuard cobre task sem issue com 0 resets, retry com 0 resets e falha terminal com 1 reset, e nas linhas 298-306 asserta LITERALMENTE os tres fragmentos da guarda i.status = 'in_progress', NOT EXISTS e a lista dos quatro status ativos, o que impede alguem afrouxar a query sem quebrar teste; 307-309 confere os 2 args. E :314 TestReconcileFailedIssueBroadcastsMatchedReset assina EventIssueUpdated e exige len(published) == 1 com o WorkspaceID correto. 5) F3 e F4 seguem como antes: CreateRetryTask insere 'queued', dentro da guarda, entao o retried bool e defesa redundante e o fallback passar false na :308 e seguro; e a perda do dedup por issue custa round-trip extra sem perda de correcao, agora com o ErrNoRows tratado explicitamente no case da linha 26. TRES NOTAS NAO BLOQUEANTES, para a fila e nao para este patch: (a) broadcastIssueUpdated chama getIssuePrefix que usa context.Background() em task.go:2231 em vez do ctx da requisicao, entao esse hop perde cancelamento e deadline; funcao pre-existente, porem agora esta num caminho novo e mais quente. (b) broadcastIssueUpdated faz s.Bus.Publish sem guarda de nil; verifiquei que NAO e defeito de producao, porque as duas unicas construcoes de TaskService fora de teste setam Bus, task.go:127 e runtime_sweeper.go:299 - mas e fragilidade de teste: o svc da linha 287 tem Bus nil e so nao entra em panico porque o mock devolve ErrNoRows na linha 259; se alguem der um resetIssue a esse mock, o teste passa a estourar. Sugiro setar Bus nele tambem. (c) internal/service/autopilot.go:578 continua consumindo HasActiveTaskForIssue e nao foi revisado, esta fora do escopo T2. NADA MUTADO: somente leitura, nenhuma edicao, deploy, restart, rerun, commit ou push; go test, vet e build nao executados por mim.
