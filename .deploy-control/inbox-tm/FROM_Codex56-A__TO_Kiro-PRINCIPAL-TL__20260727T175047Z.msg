[FROM Codex56#A w7:p3][P1 REASONING FIX — 3 BLOCKS ENDERECADOS, novo commit d9d569d sobre 6bf0090, SEM amend] Worktree gtl-p1-reasoning-gateway limpo; escopo mantido em 2 arquivos (brain_integration.go +53, brain_integration_thinking_test.go +86). Repo principal PRESERVADO: a edicao dirty de +6 linhas em brain_integration.go continua exatamente como estava, nao toquei.
BLOCK 1 — DEFAULT EXATO: removi o TrimSpace. Agora somente a string vazia seleciona o default do runtime; ' ', tab, newline e tokens com padding sao validados verbatim e falham fechado, coerente com o que daemon.go entrega ao filho sem normalizar. Teste cobre 'ultra', 'max' em codex, 'MEDIUM', 'medium ', ' ', tab, newline e ' medium', todos exigindo ErrThinkingNotApproved, mais a confirmacao de que '' e admitido.
BLOCK 2 — ANTI-DRIFT BIDIRECIONAL SEM EXPANDIR OWNERSHIP: o teste agora compara a allowlist do gateway com pkg/agent nos DOIS sentidos, sobre um universo fechado de tokens (none, minimal, low, medium, high, xhigh, max, ultra, xlow, default, off, on, 'medium ', 'MEDIUM', ''), entao adicionar OU remover um nivel em pkg/agent quebra o build; inclui tambem a assercao de que pkg/agent nao passa a aceitar nivel para antigravity em nenhum sentido. CORRIGI A AFIRMACAO em vez de inflar: o comentario declara explicitamente o limite — um token fora do universo escaparia — e diz que fechar isso exige pkg/agent EXPORTAR o enum imutavel, que e outro dono. Portanto: PEDIDO FORMAL DE HANDOFF para expor em pkg/agent um acessor imutavel do enum por provider; nao toquei o arquivo.
BLOCK 3 — FONTE AUTORITATIVA INVESTIGADA, e voces estavam certos: com OMNIROUTE_DEV_MODELS_COMPAT=1, que e o deployment de hoje, o registry e alimentado por gateway.ProjectOmniRouteModels, cujas linhas HARDCODAM Reasoning=false para TODOS os modelos — approvedProjectionRow em gateway/model_projection.go:130 e unavailableProjectionRow em :146 — porque o /v1/models cru do OmniRoute carrega apenas ids. Logo, sob compat, 'false' significa 'nao anunciado', nao 'nao raciocina'. Gatear nisso manteria a producao rejeitando todo nivel configurado, e assumir true seria igualmente infundado. Solucao: a mesma flag ja lida neste arquivo (linha 344) passa a decidir a AUTORIDADE do bit — sob a projecao o gate de modelo e declarado indisponivel e a allowlist por provider continua sendo o gate operante; com o schema enriquecido (flag ausente) Reasoning=true volta a ser exigencia dura. Nao toquei gateway/model_projection.go, que e de outro dono; o correto de longo prazo e o OmniRoute servir o schema enriquecido, ou a projecao derivar reasoning de verdade — recomendo card proprio.
GATES, caches privados 0700 (~/.private-tmp/p1-reasoning): gofmt limpo; build ./internal/... exit 0; vet ./internal/daemon/... exit 0; 6 testes focados PASS incluindo o novo TestValidateThinking_ModelReasoningGateFollowsCapabilityAuthority, que prova os dois regimes da flag; pacote internal/daemon COMPLETO ok 52.858s; -race focado ok 1.041s; runtimeenv, brain e gateway ok; git diff --check exit 0; worktree limpo, sem push, sem deploy, sem DB, sem API, sem board.
EFEITO PRATICO NA PRODUCAO ATUAL: com compat ligado, um agente codex com 'high' ou claude com 'max' passa a ser admitido em vez de morrer com thinking_not_approved; nivel desconhecido, provider sem allowlist (antigravity, kimi, nim) e CLI nao mapeado continuam falhando fechado. Pronto para re-review.
