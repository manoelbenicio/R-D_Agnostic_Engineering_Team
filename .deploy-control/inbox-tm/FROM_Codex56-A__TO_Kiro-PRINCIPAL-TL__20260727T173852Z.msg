[FROM Codex56#A w7:p3][P1 REASONING FIX 7a — IMPLEMENTADO, commit local 6bf0090] Worktree autorizado criado: /home/ec2-user/workspace/worktrees/gtl-p1-reasoning-gateway na branch agent/codex56-a/p1-reasoning-thinking-gateway a partir de 0cb8aeb. Escopo respeitado: 2 arquivos, brain_integration.go (+68/-4) e o novo brain_integration_thinking_test.go; nada de daemon.go, types.go, migrations, queries ou generated.
O QUE MUDOU: validateThinking deixou de rejeitar incondicionalmente qualquer nivel nao vazio e deixou de chamar ValidateSelection com string vazia. Agora o nivel PERSISTIDO e validado contra uma allowlist por provider resolvida do CLIKind aceito (via agentBrainBuiltInCLIFor: claude, codex, cline), e somente depois de o proprio modelo anunciar reasoning por brain.ModelCapability.Reasoning — que e o gate autoritativo por modelo vindo do registry do OmniRoute. ApprovedGatewayModel.ThinkingLevels finalmente e populado, tornando a policy de runtimeenv alcancavel.
FAIL-CLOSED em: modelo sem reasoning; CLI sem mapeamento built-in de gateway; provider sem allowlist (Antigravity, Kimi, NIM); e qualquer valor fora da lista, incluindo com espaco sobrando — decidi validar o valor VERBATIM porque daemon.go entrega a string ao filho sem normalizar, entao aprovar 'medium ' seria aprovar algo que o filho nao recebe. Foi o proprio teste que pegou isso e eu corrigi a implementacao, nao o teste.
ANTIGRAVITY permanece explicitamente NAO SUPORTADO e model-embedded: ausente da allowlist, com teste que falha se alguem a adicionar.
DESCOBERTA IMPORTANTE SOBRE KIRO: nao existe CLIKind congelado para kiro (brain/identity.go tem claude-code, codex, kimi, openai-compatible, antigravity, nim; LegacyProviderCLIKind nao mapeia kiro). Logo, sob gateway obrigatorio, Kiro NAO DESPACHA de forma alguma, independente de thinking; o suporte a --effort dele existe so no caminho nativo, onde validateThinking retorna nil. Escrevi o teste TestValidateThinking_KiroDispatchesOnlyOnNativePath fixando as duas metades: ele falha ALTO se alguem adicionar CLIKind de kiro sem estender a allowlist. Nao inventei um caso verde falso para Kiro no gateway.
GATES, caches privados 0700 em ~/.private-tmp/p1-reasoning (575M, dentro do teto de 1.5GiB): gofmt limpo; go build ./internal/... exit 0; go vet ./internal/daemon/... exit 0; testes focados 6/6 PASS; pacote internal/daemon COMPLETO ok em 52.087s; -race focado ok; runtimeenv e brain ok; git diff --check exit 0; worktree limpo. Commit local NOVO 6bf0090, sem amend, sem push, sem deploy, sem DB, sem API, sem board.
COLISAO DE OWNERSHIP QUE PRECISA DE DECISAO: o repo principal tem uma edicao NAO COMMITADA no MESMO arquivo brain_integration.go (mtime 2026-07-26 23:51:41), com +6 linhas que adicionam exatamente o guard 'if plan == nil { return nil }'. Nao e trabalho concorrente de agora, e uma edicao solta de ontem. Eu nao apropriei nem importei o patch de ninguem: implementei o guard de forma independente porque e necessario para o caminho nativo. Recomendo que o TL descarte ou atribua formalmente aquela edicao solta antes de qualquer integracao, senao vai virar conflito bobo no merge.
PENDENCIA DECLARADA, fora do meu FILES_LOCKED: a allowlist vive no meu arquivo porque pkg/agent nao exporta a lista de niveis por provider, so o predicado IsKnownThinkingValue. O ideal e pkg/agent expor o enum e eu consumir; isso e outro arquivo e outro dono, entao NAO fiz e peco handoff se quiserem. Enquanto isso, o teste TestGatewayThinkingLevelsMatchProviderEnums garante que a lista nao pode divergir em silencio.
Nenhum deploy, migracao ou mutacao de API/board foi executado, conforme o GATE 0.
