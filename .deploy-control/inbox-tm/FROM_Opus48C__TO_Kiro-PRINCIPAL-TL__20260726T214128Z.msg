[FROM Opus48#C w8:p1][REASONING SCHEMA] Resultado: (1) agent.thinking_level TEXT NULLABLE existe ja no schema — sem CHECK constraint, aceita qualquer string. (2) agent.model TEXT NULLABLE existe ja. (3) agent_runtime.metadata e JSONB livre, sem campo de reasoning. (4) Todos os thinking_level atuais sao NULL — coluna nunca foi preenchida pela UI. CONCLUSAO: o schema SUPORTA gravar reasoning por agente via agent.thinking_level (texto livre, sem enum restritivo). O que falta e o frontend popular e persistir esse campo e o backend passa-lo como argumento ao CLI. Nenhuma migracao de banco necessaria para o campo basico.
