[FROM Codex56#A w7:p3][ORQ-17 STAGE3B V2 — VEREDITO FINAL INDEPENDENTE: PASS no escopo L1/L2/L3/S1/S2/self-deadlock] Independente do Review1: nao li o parecer dele, decidi por leitura direta do codigo com hash conferido agora — helper fabc8f85… e runner c3d64f85… inalterados.
CONFIRMACAO 1, GUARD SO FILA: helper:156 e literalmente 'LOCK TABLE agent_task_queue IN SHARE MODE' e e a UNICA instrucao LOCK do guard; o comentario :154-155 declara a intencao ('The guard freezes queue admission only. member is locked by the mutation transaction itself, avoiding any cross-connection self-deadlock'). Grep exaustivo de 'LOCK TABLE' no helper retorna somente 3 ocorrencias: :156 (guard, fila), :189 (mutacao provision) e :191 (mutacao rollback). Nao existe mais lock de member cross-connection.
CONFIRMACAO 2, member DENTRO DAS DUAS ROTAS DE beginMutation: :189 'LOCK TABLE "user", user_password_credential, member IN SHARE ROW EXCLUSIVE MODE' para provision, e :191-194 a lista de rollback que inclui member na terceira linha, junto de agent, agent_runtime, chat_session, feedback, github_installation, lark_installation, notification_preference, personal_access_token, pinned_item, skill, task_token e workspace_invitation, todos em SHARE ROW EXCLUSIVE. Como member passa a ser travado pela PROPRIA transacao que deleta, o DELETE cascateado (ROW EXCLUSIVE) nao conflita — a transacao nao conflita consigo mesma — e o modo continua auto-exclusivo, preservando o congelamento contra insercao concorrente. A verificacao de membership zero permanece no guard, :164-165 com E_MEMBER_T1.
L1/L2/L3/S1/S2: PASS, conforme minha verificacao anterior sobre os mesmos hashes (breakglass com recibo+SHA; tokens distintos de commit-com-guard-falho e leitura obrigatoria de contagens; gate HTTP contido /auth/login=200 e /api/me=200; printf builtin; ulimit -c 0 com SC3045 inocuo porque /bin/sh->bash em ORQ1).
LIMITES: nao reproduzi o binario 1710e010…, nao resolvi segredo, nao chamei asm-exec, nao executei SQL nem docker compose run, nao mutei board; a prova do lock e derivada da tabela 13.2 do PostgreSQL 17 e do codigo, nao de execucao. Fora deste escopo e ainda abertos do meu parecer anterior: o trap do runner apaga os arquivos capturados e destroi o token E_* fixo, e o mkdir sem guarda emite diagnostico bruto em reexecucao. PASS no escopo; execucao real segue dependendo da autorizacao one-time do owner.
