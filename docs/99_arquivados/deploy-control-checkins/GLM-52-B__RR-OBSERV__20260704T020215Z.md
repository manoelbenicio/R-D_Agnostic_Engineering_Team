# Check-in: GLM#52#B — RR-OBSERV

- agent: GLM#52#B
- stream: RR-OBSERV (Savings KPI + spend dimensions in dashboards-as-code + Savings formula doc)
- started_at: 2026-07-04T02:02:15Z
- status: IN_PROGRESS
- files_locked (NEW / isolated only):
  - scripts/observability/components.savings.yaml                       (NEW — overwriting any pre-existing stub)
  - deploy/observability/grafana/dashboards/savings.generated.json      (GENERATED by gen_dashboards.py)
  - docs/project/savings-kpi.md                                         (NEW — overwriting any pre-existing stub)

## Grounding
- PROMPT_GLM52-2_RR-OBSERV.md read: Wave-1 stream executed LITERALLY to completion;
  mandatory sign-in/sign-out (priority 0); reuse gen_dashboards.py unmodified (prefer a
  new YAML spec fed to it); do NOT edit product Go or existing dashboards.
- design.md §8 read (Observabilidade + aba SAVINGS = KPI DO MOAT: "$ economizado rotacionando
  assinaturas vs metered"). §11 decision 1: Requesty = referência, nunca vendor; zero custo
  metered em produção → tokens served on subscriptions = tokens that would otherwise be billed
  metered. Savings KPI is NEW; rule: invent no metric names.
- gen_dashboards.py read (reused UNMODIFIED): validates panel.metric against CATALOG_METRICS
  (the 11 real metrics from credential_metrics.go), panel.type in {timeseries,stat,gauge,table},
  and requires the declared metric string to literally appear in the query. Output file =
  `<component.name>.generated.json`; dashboard title = component.title; dashboard uid =
  component.name.
- credential_metrics.go read — REAL catalog (NO metric invented), 11 metrics:
    rotation_total{vendor,reason,result}, rotation_duration_seconds{vendor},
    all_accounts_exhausted{vendor}, accounts_available{vendor},
    account_status{vendor,account_id,status}, account_tokens_used{vendor,account_id},
    account_window_seconds_remaining{vendor,account_id}, exhaustion_detected_total{vendor,signal},
    credential_restore_total{vendor,result}, cred_env_injection_total{vendor,result},
    credential_prepare_seconds{vendor}.
- Grafana container multica-grafana UP; API http://localhost:3000; secret
  deploy/observability/secrets/grafana_admin_password (MASKED in ALL pasted output).

## Plan
1. Author components.savings.yaml (overwrite stub): a "Savings (estimated)" panel built as a
   DOCUMENTED DERIVED expression on the real gauge account_tokens_used × hypothetical blended
   metered price P_metered (constant embedded in the PromQL query) — marked ESTIMATED in the
   panel title; plus exact spend-dimension panels straight from the real catalog
   (accounts_available by vendor, rotation_total by reason, all_accounts_exhausted by vendor,
   exhaustion_detected_total by signal, account_tokens_used table). No fabricated metric.
2. Run `python3 scripts/observability/gen_dashboards.py scripts/observability/components.savings.yaml`
   → deploy/observability/grafana/dashboards/savings.generated.json.
3. Author docs/project/savings-kpi.md (overwrite stub): Savings formula, its inputs, what it
   proves (the moat in $), and the gap (which inputs still need instrumentation).
4. Verify: python3 json.load of generated json = ok; docker restart multica-grafana;
   `curl -s -u admin:"$PW" http://localhost:3000/api/search?query=Savings` -> found>=1 (PW MASKED).
5. Sign out this SAME board file with finished_at + status:DONE + build_result (password masked).

## Notes
- No product Go edited; no existing dashboards edited; gen_dashboards.py reused unmodified;
  no git commit. Savings estimate uses a documented in-query constant P_metered (gap flagged in
  savings-kpi.md). Until the metered-price side is a real series, Savings stays ESTIMATED and the
  computable spend-dimension panels ship exact.

--- SIGN OUT ---
finished_at: 2026-07-04T02:04:38Z
agent confirmed: GLM#52#B
status: DONE

deliverables (NEW / overwrote pre-existing stubs with own authorship):
  - scripts/observability/components.savings.yaml (NEW) — 8 panels, ONLY real catalog
    metrics from server/internal/metrics/credential_metrics.go; the two Savings panels
    are a documented derived expression (sum(account_tokens_used) * P_metered) marked
    ESTIMATED in their titles. No metric invented.
  - deploy/observability/grafana/dashboards/savings.generated.json (GENERATED by
    gen_dashboards.py, reused unmodified) — valid JSON, 8 panels, loaded by Grafana.
  - docs/project/savings-kpi.md (NEW) — Savings formula, inputs, moat-in-$, and the
    gap (per-vendor metered price + model/context weighting + prompt/completion split
    not yet instrumented). No fabricated metric.

build_result (GREEN — password MASKED throughout):
```
$ cd /mnt/c/VMs/Projetos/Automonous_Agentic/multica-auth-work
$ python3 scripts/observability/gen_dashboards.py scripts/observability/components.savings.yaml
wrote deploy/observability/grafana/dashboards/savings.generated.json

$ python3 -c "import json; d=json.load(open('deploy/observability/grafana/dashboards/savings.generated.json')); print('json ok'); print('title=', d['title'], '; uid=', d['uid'], '; panels=', len(d['panels']))"
json ok
title= Savings KPI (Moat) ; uid= savings ; panels= 8

generated dashboard panels (8 — only real catalog metrics; Savings panels marked ESTIMATED):
  1 stat        | Savings vs metered (ESTIMATED, USD)        [account_tokens_used * 3e-6]
  2 timeseries  | Savings by vendor (ESTIMATED, USD)         [account_tokens_used * 3e-6]
  3 timeseries  | Tokens used by vendor (observed spend proxy) [account_tokens_used]
  4 table       | Token usage by account (savings feedstock)   [account_tokens_used]
  5 timeseries  | Accounts available by vendor                 [accounts_available]
  6 stat        | Rotations by reason                          [rotation_total]
  7 gauge       | All accounts exhausted by vendor             [all_accounts_exhausted]
  8 timeseries  | Exhaustion detected by signal                [exhaustion_detected_total]

$ docker restart multica-grafana >/dev/null && echo 'restarted multica-grafana' && sleep 8
restarted multica-grafana

$ PW=$(cat deploy/observability/secrets/grafana_admin_password)   # MASKED: ****
$ echo "PW loaded (MASKED: ****), length=${#PW}"
PW loaded (MASKED: ****), length=23

$ curl -s -u admin:"$PW" "http://localhost:3000/api/search?query=Savings" | python3 -c "import sys,json; r=json.load(sys.stdin); print('found:', len(r)); [print(' -', x.get('title'),'| uid=',x.get('uid'),'| type=',x.get('type')) for x in r]"
found: 1
 - Savings KPI (Moat) | uid= savings | type= dash-db
```

verification summary:
  - gen_dashboards.py validated every panel.metric against CATALOG_METRICS (the 11 real
    metrics from credential_metrics.go), panel.type in {timeseries,stat,gauge,table}, and
    that the declared metric string literally appears in each query — exited 0 -> only real
    metrics used; NO metric invented.
  - json.load of savings.generated.json = ok (title= Savings KPI (Moat), uid= savings, 8 panels).
  - Savings panels marked ESTIMATED in titles (price side is an in-query constant P_metered=3e-6,
    gap documented in savings-kpi.md).
  - docker restart multica-grafana done; Grafana API search?query=Savings -> found: 1 (>=1). PASS.
  - Grafana admin password read from deploy/observability/secrets/grafana_admin_password,
    never printed (MASKED: ****, only length=23 shown).

notes:
  - No product Go edited; no existing dashboards edited; gen_dashboards.py reused unmodified;
    no git commit.
  - Savings estimate uses a documented in-query constant P_metered=3e-6 USD/token; primary gap
    (per-vendor metered reference price) + model/context weighting + prompt/completion split
    flagged in savings-kpi.md. Until then Savings stays ESTIMATED; spend dimensions ship exact.
