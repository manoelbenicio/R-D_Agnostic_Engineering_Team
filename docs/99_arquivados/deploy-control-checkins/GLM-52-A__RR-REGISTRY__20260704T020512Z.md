# Deploy Control Board — GLM-52-A / RR-REGISTRY

- agent: GLM#52#A
- stream: RR-REGISTRY (Wave-1)
- started_at: 20260704T020512Z (UTC)
- status: SUPERSEDED_BY_DONE_CHECKIN
- workspace: /mnt/c/VMs/Projetos/Automonous_Agentic/multica-auth-work
- design_ref: /mnt/c/VMs/Projetos/Automonous_Agentic/openspec/changes/rotation-router/design.md (§9 — Account Registry + Governança)
- next_migration: 124
- files_locked (NEW, authored by GLM#52#A):
  - server/migrations/124_approved_accounts.up.sql
  - server/migrations/124_approved_accounts.down.sql
  - scripts/staging/registry_query.sql
- do_not_touch: migration 123 (rotation), all existing migrations <124, product Go code
- conventions_source: server/migrations/123_rotation.up.sql (uuid PK gen_random_uuid(), TIMESTAMPTZ, FK ON DELETE CASCADE, CHECK on enum-like TEXT, named idx_* indexes, no comments; down = DROP TABLE IF EXISTS)
- spec (table approved_accounts):
    approved_id    uuid pk default gen_random_uuid(),
    tenant_id      uuid not null,
    account_id     uuid not null references accounts(account_id) on delete cascade,
    allowed        boolean not null default true,
    worktype_scope text  CHECK in (GENERAL|HEAVY|CHEAP|REVIEW),  -- optional, null=all
    created_at     timestamptz not null default now(),
    unique(tenant_id, account_id)
  + index on (tenant_id). down = DROP TABLE IF EXISTS approved_accounts.
  registry_query.sql: accounts LEFT JOIN approved_accounts, per-tenant approval + vendor/status/priority.
- plan:
  1. Confirm/ensure the 3 locked files are authored to spec (match 123 style; real columns only from accounts).
  2. Live verify on multica-postgres-1 (db=multica, user=multica):
     down(clean slate) -> up -> \d approved_accounts -> down -> \dt approved_accounts(gone) -> up(re-apply, leave applied) -> registry_query.sql
  3. Sign-out this SAME file: finished_at(UTC), agent confirmed, status DONE, build_result = pasted psql outputs.

---

# Sign-out

- agent: GLM#52#A (confirmed)
- started_at: 20260704T020512Z (UTC)
- finished_at: 20260704T020810Z (UTC)
- status: DONE
- target: multica-postgres-1 (db=multica, user=multica) — Up 36 minutes (healthy)
- prompt_ref: /mnt/c/VMs/Projetos/Automonous_Agentic/agentic-prompts-hub/archive/PROMPT_GLM52-1_RR-REGISTRY.md (moved from new_prompts/; spec byte-identical to the executed task)
- files_authored (NEW, by GLM#52#A; already in place to spec — no stub overwrite needed this run):
  - server/migrations/124_approved_accounts.up.sql   (table approved_accounts + idx_approved_accounts_tenant)
  - server/migrations/124_approved_accounts.down.sql (DROP TABLE IF EXISTS approved_accounts)
  - scripts/staging/registry_query.sql               (accounts LEFT JOIN approved_accounts, per-tenant)
- conventions_matched: 123_rotation.up.sql (uuid PK gen_random_uuid(), TIMESTAMPTZ, FK ON DELETE CASCADE, CHECK on enum-like TEXT, named idx_* indexes, no-comment DDL; down = DROP TABLE IF EXISTS).
- columns_used (real only): accounts.{account_id, tenant_id, vendor, status, priority}; approved_accounts.{allowed, worktype_scope, created_at}. No invented columns.
- do_not_touch honored: migration 123 + all <124 untouched; product Go untouched; schema_migrations not touched by raw psql (123's up.sql likewise does not insert into it — the app migrate framework records versions).
- final_db_state: approved_accounts table LEFT APPLIED (re-applied in STEP 6, confirmed present in FINAL \dt).

## build_result (live psql outputs, verbatim)

### PRE: accounts row count
```
 accounts_total
----------------
              4
(1 row)
```

### STEP 1: down (clean slate)  ->  124_approved_accounts.down.sql
```
DROP TABLE
```

### STEP 2: up  ->  124_approved_accounts.up.sql
```
CREATE TABLE
CREATE INDEX
```

### STEP 3: \d approved_accounts
```
                           Table "public.approved_accounts"
     Column     |           Type           | Collation | Nullable |      Default
----------------+--------------------------+-----------+----------+-------------------
 approved_id    | uuid                     |           | not null | gen_random_uuid()
 tenant_id      | uuid                     |           | not null |
 account_id     | uuid                     |           | not null |
 allowed        | boolean                  |           | not null | true
 worktype_scope | text                     |           |          |
 created_at     | timestamp with time zone |           | not null | now()
Indexes:
    "approved_accounts_pkey" PRIMARY KEY, btree (approved_id)
    "approved_accounts_tenant_id_account_id_key" UNIQUE CONSTRAINT, btree (tenant_id, account_id)
    "idx_approved_accounts_tenant" btree (tenant_id)
Check constraints:
    "approved_accounts_worktype_scope_check" CHECK (worktype_scope = ANY (ARRAY['GENERAL'::text, 'HEAVY'::text, 'CHEAP'::text, 'REVIEW'::text]))
Foreign-key constraints:
    "approved_accounts_account_id_fkey" FOREIGN KEY (account_id) REFERENCES accounts(account_id) ON DELETE CASCADE
```
=> columns + PK(approved_id) + UNIQUE(tenant_id,account_id) + idx on tenant_id + CHECK(worktype_scope) + FK->accounts(account_id) ON DELETE CASCADE all present. Matches spec + 123 style.

### STEP 4: down
```
DROP TABLE
```

### STEP 5: \dt approved_accounts (expect gone)
```
(stderr) Did not find any relation named "approved_accounts".
```
=> table removed. GONE confirmed (psql prints this note + empty result when \dt <name> finds no match).

### STEP 6: up (re-apply, leave applied)
```
CREATE TABLE
CREATE INDEX
```

### STEP 7: registry_query.sql  ->  scripts/staging/registry_query.sql
```
              tenant_id               |              account_id              | vendor |  status   | priority | approved | worktype_scope | approved_at
--------------------------------------+--------------------------------------+--------+-----------+----------+----------+----------------+-------------
 20000000-0000-4000-8000-000000000001 | 42dc3464-7883-52d2-9296-062327d2ff1c | codex  | available |        1 | f        |                |
 20000000-0000-4000-8000-000000000001 | c93f0194-34c8-5d92-8aab-65cc36aa3327 | codex  | available |        1 | f        |                |
 4f10dde6-9f68-442b-a3b3-1befffc9c778 | 10000000-0000-4000-8000-000000000002 | codex  | available |        2 | f        |                |
 4f10dde6-9f68-442b-a3b3-1befffc9c778 | 10000000-0000-4000-8000-000000000001 | codex  | available |        1 | f        |                |
(4 rows)
```
=> query runs without error; LEFT JOIN shows all 4 real accounts per tenant with vendor/status/priority; approved=f (no approved_accounts rows yet, COALESCE->false); worktype_scope/approved_at NULL. Correct.

### FINAL: \dt approved_accounts (expect present — left applied)
```
              List of relations
 Schema |       Name        | Type  |  Owner
--------+-------------------+-------+---------
 public | approved_accounts | table | multica
(1 row)
```
=> approved_accounts LEFT APPLIED in live DB.

## verdict
ALL GREEN. up creates (columns + FK + UNIQUE + index + CHECK), down removes (\dt gone), registry_query.sql runs clean against real accounts data. Migration left applied. Reproducible across runs. No partial hand-back.