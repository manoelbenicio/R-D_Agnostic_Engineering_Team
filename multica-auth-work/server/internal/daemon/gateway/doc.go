// Package gateway defines the unwired OmniRoute client and routing contracts
// consumed by Agent Brain. It never reads provider credentials or the gateway
// secret file; authentication is supplied through a narrow injected callback.
package gateway
