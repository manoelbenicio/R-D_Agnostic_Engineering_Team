# Docker secrets — NUNCA commite credenciais reais. Acompanhe apenas *.example.
/secrets/grafana_admin_password
/secrets/pg_user
/secrets/pg_pass

# Overrides locais de env (a raiz do repo ja ignora .env*).
/.env
