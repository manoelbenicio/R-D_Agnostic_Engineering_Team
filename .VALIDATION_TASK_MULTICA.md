# VALIDATION TASK — Multica Deploy Independent Audit

## PAPEL
Você é um agente validador INDEPENDENTE. Sua função é verificar se o deploy do Multica foi feito
corretamente por outro agente. Você NÃO executa o deploy — você APENAS valida.

## OBJETIVO
Auditar o deploy do Multica verificando TODOS os critérios de sucesso de forma independente.
Registrar os resultados em um check-in próprio. Reportar discrepâncias.

## CHECK-IN
Crie na RAIZ DO PROJETO o arquivo:
    /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/CHECKIN_Codex55A_VALIDATOR_<UTC-ISO8601>.md

## VALIDAÇÕES A EXECUTAR

### V1 — Verificar que o repo está atualizado
```bash
cd /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team
git log --oneline -3
git status --short
```
Esperado: branch main, limpo, no último commit de origin/main.

### V2 — Verificar estrutura da aplicação
```bash
ls -la multica-auth-work/Makefile multica-auth-work/docker-compose.selfhost.yml
```
Esperado: ambos existem.

### V3 — Verificar containers rodando
```bash
cd multica-auth-work
docker compose -f docker-compose.selfhost.yml ps
```
Esperado: 3 serviços rodando (postgres healthy, backend, frontend).

### V4 — Health checks independentes
```bash
curl -s http://localhost:8080/health
curl -s http://localhost:8080/readyz
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:3000
```
Esperado: health ok, readyz com db:ok e migrations:ok, frontend retorna 200.

### V5 — Verificar .env gerado
```bash
ls -la multica-auth-work/.env
```
Esperado: arquivo existe (NÃO mostrar conteúdo — contém segredos).

### V6 — Revisar o check-in do deploy agent
```bash
cat /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/CHECKIN_Codex55B_*.md
```
Esperado: todos os passos 1-5 com status DONE e evidência real. Se algum estiver BLOCKED, reportar.

### V7 — Verificar logs de migration
```bash
cd multica-auth-work
docker compose -f docker-compose.selfhost.yml logs backend 2>&1 | grep -i "migrat" | tail -20
```
Esperado: migrations aplicadas sem erros.

## FORMATO DE RESPOSTA
Ao final, responda com:
1. Status de cada validação (V1-V7): PASS / FAIL / WARN
2. Caminho do seu arquivo de check-in
3. Lista de discrepâncias encontradas (se houver)
4. Veredito final: DEPLOY VALIDADO ou DEPLOY COM PROBLEMAS

## REGRAS
- NÃO altere nada. Apenas leia e verifique.
- NÃO exponha segredos.
- Se algo falhar, registre como FAIL com evidência exata.
