---
phase: 02-forkmap
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [docs/prodex/prodex-invariants.md, docs/prodex/prodex-fork-map.md]
autonomous: true
requirements: [REQ-09, REQ-27, REQ-28, REQ-29, REQ-30, REQ-31, REQ-32]
user_setup: []
must_haves:
  truths:
    - "Hard affinity invariant documented and traced to crates"
    - "Rotate-before-commit invariant documented"
    - "Profile auth isolation invariant documented"
    - "Runtime broker mapped to L2 contract operations"
    - "Memory, cookies, quota, caveman scoped with security assessment"
  artifacts:
    - path: "docs/prodex/prodex-invariants.md"
      provides: "Invariants traced to source crates"
  key_links: []
---

<objective>
Document runtime invariants and map remaining gap crates (broker, memory, cookies, quota, caveman).

Purpose: Invariants must be preserved in any fork. Gap crates need scope decisions before integration.
Output: Invariants document + gap crate analysis + GATE P2.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-forkmap/02-01-SUMMARY.md
@Diligencias/02_FORKMAP_P2.md
@Diligencias/00c_PRODEX_CRATE_COVERAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document preserved invariants</name>
  <files>docs/prodex/prodex-invariants.md</files>
  <read_first>Diligencias/02_FORKMAP_P2.md, docs/prodex/prodex-fork-map.md</read_first>
  <action>
    Create docs/prodex/prodex-invariants.md:

    1. Hard affinity: previous_response_id, turn-state, session_id
       → Crates: prodex-session-store, prodex-runtime-proxy
       → Test: session with continuation preserves state across requests

    2. Rotate-before-commit: never rotate mid-stream after output begins
       → Crates: prodex-runtime-proxy, prodex-runtime-policy
       → Test: rotation only at session boundary

    3. Profile auth isolation: $PRODEX_HOME/profiles/<name>
       → Crates: prodex-profile-identity, prodex-profile-export, prodex-shared-codex-fs
       → Test: two profiles never share credentials

    4. Smart Context integrity: fallback exact when structural risk
       → Crates: prodex-context
       → Test: structural change triggers fallback, not corruption
  </action>
  <verify>grep -c "invariant" docs/prodex/prodex-invariants.md</verify>
  <acceptance_criteria>
    - "4 invariants documented with crate references"
    - "Each invariant has a testable property"
  </acceptance_criteria>
  <done>Invariants documented and traced to crates</done>
</task>

<task type="auto">
  <name>Task 2: Map gap crates + GATE P2</name>
  <files>docs/prodex/prodex-fork-map.md</files>
  <read_first>docs/prodex/prodex-fork-map.md, Diligencias/00c_PRODEX_CRATE_COVERAGE.md, Diligencias/00d_CONFIG_ENV_SECURITY.md</read_first>
  <action>
    Append to docs/prodex/prodex-fork-map.md:

    1. Runtime broker (REQ-29): prodex-runtime-broker
       → Map health/registry/metrics to HealthCheck, RouteDecisionEvent, RuntimeEventStream
       → Scope: active in AS-IS; broker log is observability surface

    2. Memory Mem0 (REQ-27/37b): prodex-memory
       → Scope decision: DISABLED by default (privacy/PII risk)
       → If enabled: redaction mandatory, contrato must cover memory events

    3. Cookies (REQ-30): prodex-runtime-cookies
       → Auth/session relay — security audit required
       → Scope: document surface, flag for P4 security review

    4. Quota (REQ-31): prodex-quota, runtime-quota
       → Amplify quota_mode per vendor for proactive rotation
       → Scope: map to P5 vendor matrix

    5. Caveman (REQ-32): prodex-caveman-assets
       → SECURITY: DISABLED by default (RCE/supply-chain risk)
       → If ever used: allowlist + timeout + no external marketplace

    GATE P2:
    - [ ] Fork-map reviewed with all 44 crates
    - [ ] Invariants traced to crates
    - [ ] Gap crates scoped with security assessment
    - [ ] No secrets in documents
  </action>
  <verify>grep -q "GATE P2" docs/prodex/prodex-fork-map.md</verify>
  <acceptance_criteria>
    - "All 5 gap crate categories addressed"
    - "Memory and Caveman marked DISABLED by default"
    - "GATE P2 checklist complete"
  </acceptance_criteria>
  <done>GATE P2 closed — fork-map complete for future milestone</done>
</task>

</tasks>

<verification>
- [ ] 4 invariants documented with crate traces
- [ ] Gap crates (REQ-27..32) all addressed
- [ ] Security-sensitive crates flagged
- [ ] GATE P2 checklist complete
</verification>

<success_criteria>
- Fork-map is comprehensive analysis artifact
- Invariants are clear for preservation in fork
- Gap crates have scope decisions
</success_criteria>

<output>
After completion, create `.planning/phases/02-forkmap/02-02-SUMMARY.md`
</output>
