---
phase: 04-state-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-10, REQ-03, REQ-11, REQ-12, REQ-33, REQ-34, REQ-37, REQ-37b]
user_setup: []
must_haves:
  truths:
    - "No SQLite for shared state (Postgres only)"
    - "Migrations are reversible (up/down tested)"
    - "No secret appears in logs/traces/errors/audit"
    - "Audit taxonomy covers 5 event types"
    - "PRODEX_ALLOW_UNSAFE_CHILD_ENV is OFF"
    - "Caveman/hook is DISABLED by default"
  artifacts:
    - path: "docs/security/redaction-policy.md"
      provides: "Redaction rules for all log surfaces"
    - path: "docs/security/audit-taxonomy.md"
      provides: "Audit event type definitions"
    - path: "docs/security/prodex-env-inventory.md"
      provides: "Complete PRODEX_* env var inventory with secure defaults"
  key_links: []
---

<objective>
Validate state backend (Postgres, no SQLite), implement redaction, audit taxonomy, and ENV security inventory.

Purpose: Security and state integrity are non-negotiable invariants. Every surface must be audited before QA.
Output: Security documentation + redaction policy + audit taxonomy + ENV inventory.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Diligencias/04_STATE_SECURITY_P4.md
@Diligencias/00d_CONFIG_ENV_SECURITY.md
@Diligencias/00e_COMPLETENESS_REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Postgres backend + reversible migrations</name>
  <read_first>Diligencias/04_STATE_SECURITY_P4.md</read_first>
  <action>
    1. Verify no SQLite for shared state: grep -r "sqlite" server/ (must be empty or test-only)
    2. Verify Postgres used for gateway/ledger/approved-accounts
    3. Confirm 322 .sql migrations have up/down pairs
    4. Test reversible: pick migration 124 (approved_accounts), run up then down then up
    5. Record evidence in .deploy-control/evidence/p4-no-sqlite.md
  </action>
  <verify>grep -rL "sqlite" server/internal/ | head -5</verify>
  <done>Postgres confirmed, SQLite absent, migrations reversible</done>
</task>

<task type="auto">
  <name>Task 2: Redaction policy + Audit taxonomy</name>
  <files>docs/security/redaction-policy.md, docs/security/audit-taxonomy.md</files>
  <read_first>Diligencias/04_STATE_SECURITY_P4.md</read_first>
  <action>
    1. Create redaction policy: tokens, JWTs, ghp_*, API keys MUST be masked in all surfaces
    2. Test: inject fake token "ghp_ABCDEF123456" and verify masked in output
    3. Map prodex-presidio + prodex-redaction as native redaction engines
    4. Create audit taxonomy with 5 event types:
       - account_selection, redeem_attempt, fallback_triggered
       - continuation_binding, context_rewrite_decision
  </action>
  <done>Redaction policy and audit taxonomy documented</done>
</task>

<task type="auto">
  <name>Task 3: PRODEX_* ENV inventory + Caveman/Browser security</name>
  <files>docs/security/prodex-env-inventory.md</files>
  <read_first>Diligencias/00d_CONFIG_ENV_SECURITY.md</read_first>
  <action>
    1. Complete inventory of all PRODEX_* env vars with secure defaults
    2. PRODEX_ALLOW_UNSAFE_CHILD_ENV=off (mandatory)
    3. API keys via secret-store only, never in logs
    4. Caveman/hook: DISABLED by default (RCE/supply-chain risk)
    5. Browser automation (Playwright): scope decision (disabled/sandboxed)
    6. Memory Mem0: disabled by default, PII/privacy implications documented
    7. Cookie relay: security surface documented for audit

    GATE P4: no-SQLite + redaction smoke + audit taxonomy + ENV inventory
  </action>
  <done>ENV inventory complete with secure defaults, GATE P4</done>
</task>

</tasks>

<verification>
- [ ] No SQLite for shared state
- [ ] Migrations reversible (tested)
- [ ] Redaction smoke green
- [ ] Audit taxonomy with 5 event types
- [ ] PRODEX_* inventory complete
- [ ] Caveman DISABLED by default
</verification>

<output>After completion, create `.planning/phases/04-state-security/04-01-SUMMARY.md`</output>
