---
phase: 03-integracao
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - openspec/changes/rotation-parity-polyglot/tasks.md
  - .planning/phases/03-integracao/03-03-SUMMARY.md
autonomous: true
requirements: [REQ-40]
must_haves:
  truths:
    - "Early Rotation fires on pre-exhaustion banner in the live MessageText loop, BEFORE hard-stop"
    - "warnbanner parser per-vendor (Codex confirmed; Kiro/Antigravity mapped not-validated — no invented strings)"
    - "Distinct from reactive 'limit reached' (reactive path unchanged)"
    - "Single-router invariant preserved (legacyGoRotationAllowed guard)"
    - "Idempotent (rotationTriggered CAS); backward-compat (no rotation configured = AS-IS)"
    - "Container build/vet/test green for daemon + l2runtime + rotation"
  artifacts:
    - path: ".planning/phases/03-integracao/03-03-SUMMARY.md"
      provides: "Verification + formal closure of task 3.6 (Early Rotation / REQ-40)"
  key_links:
    - from: "server/internal/daemon/daemon.go (case agent.MessageText -> maybeProactiveRotateOnText)"
      to: "server/internal/rotation/warnbanner.go (WarningDetector.DetectWarning)"
      via: "Live agent text inspected for pre-exhaustion banner; on detection, rotate proactively (ReasonQuotaProactive) before hard-stop"
---

<objective>
Close task 3.6 — Early Rotation (REQ-40, criticidade MÁXIMA): zero-interruption
account rotation on pre-exhaustion banner. The parser (`warnbanner.go`) and the
daemon loop integration already exist in the tree (prior streams W-WARNBANNER +
W-EARLYROT); this plan verifies them independently in the container gate and
formally closes 3.6 (mark `[x]` + summary). No Go code edits required.
</objective>

<context>
@.planning/PROJECT.md · @.planning/ROADMAP.md · @.planning/REQUIREMENTS.md
@Diligencias/03_INTEGRACAO_P3.md · @docs/project/07-early-rotation-critical.md
@docs/go-integration/sidecar-lifecycle.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify warnbanner parser (per-vendor, distinct from reactive)</name>
  <files>server/internal/rotation/warnbanner.go, warnbanner_test.go</files>
  <action>DetectWarning: Codex pre-exhaustion banner -> approaching+percentLeft+resetAt; reactive "limit reached" excluded; normal text -> false; Kiro/Antigravity mapped -> false (no invented strings); unknown vendor -> false. Tests: TestWarningDetectorCodexPercentBanner/NormalText/IgnoresReactiveLimitReached/UnknownVendor.</action>
  <verify>go test ./internal/rotation/... -run TestWarningDetector -count=1</verify>
  <done>Parser verified per-vendor with reactive exclusion</done>
</task>

<task type="auto">
  <name>Task 2: Verify daemon loop integration + early rotation trigger</name>
  <files>server/internal/daemon/daemon.go</files>
  <action>case agent.MessageText (~4288) -> d.maybeProactiveRotateOnText -> warningDetector.DetectWarning -> source="warning_banner" -> rotateTaskProactively -> rotateTaskWithReason(ReasonQuotaProactive) -> rotationService.OnExhaustion (transparent resume). d.warningDetector wired on Daemon ctor (~268). legacyGoRotationAllowed gates all paths (single-router). rotationTriggered CAS (idempotent). rotationService==nil -> no-op (AS-IS).</action>
  <verify>go test ./internal/daemon/... -count=1</verify>
  <done>Loop integration + trigger + invariant + idempotency + backward-compat verified</done>
</task>

<task type="auto">
  <name>Task 3: Verify E2E staging test (real Postgres)</name>
  <files>server/internal/daemon/staging_rotation_smoke_test.go</files>
  <action>TestStagingRotationProactiveBannerRotatesOnce (build tag staging): banner -> rotate to priority-2 account; assignment + rotation_event (ReasonQuotaProactive) + credential restored; duplicate banner no re-rotate; all-exhausted -> no rotation. Requires STG-SEED Postgres (E2E path, not in normal gate).</action>
  <verify>go test -tags=staging ./internal/daemon/... -run TestStagingRotationProactiveBannerRotatesOnce -count=1</verify>
  <done>E2E staging test present and correct</done>
</task>

<task type="auto">
  <name>Task 4: Container gate (independent verification) + formal closure</name>
  <files>openspec/changes/rotation-parity-polyglot/tasks.md, .planning/phases/03-integracao/03-03-SUMMARY.md</files>
  <action>Run container gate (IPv6 OFF): go build ./... && go vet ./internal/... && go test ./internal/daemon ./internal/l2runtime ./internal/rotation -count=1 in golang:1.26-alpine. Mark tasks.md 3.1-3.6 [x]. Create 03-03-SUMMARY.md with real gate evidence. GATE P3 (3.6): build/vet/test green + early-rotation path verified.</action>
  <verify>echo "GATE P3 (3.6): build/vet/test green; early-rotation verified"</verify>
  <done>3.6 formally closed — P3 complete</done>
</task>

</tasks>

<verification>
- [ ] warnbanner parser per-vendor with reactive exclusion (unit green)
- [ ] daemon MessageText loop calls maybeProactiveRotateOnText (live capture)
- [ ] warningDetector wired on Daemon constructor (integration live)
- [ ] early rotation uses ReasonQuotaProactive before hard-stop
- [ ] single-router invariant preserved (legacyGoRotationAllowed)
- [ ] idempotent (rotationTriggered CAS); backward-compat (AS-IS)
- [ ] container build/vet/test green (daemon + l2runtime + rotation)
- [ ] tasks.md 3.1-3.6 [x]; 03-03-SUMMARY.md with gate evidence
</verification>

<output>After completion, create `.planning/phases/03-integracao/03-03-SUMMARY.md`</output>
