---
phase: 03-integracao
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [server/internal/daemon/daemon.go, server/internal/l2runtime/client.go]
autonomous: true
requirements: [REQ-05, REQ-06]
user_setup: []
must_haves:
  truths:
    - "RuntimeEventStream ingest works (L2→Go)"
    - "Event ingest does NOT trigger Go-side rotation"
    - "Test proves single-router invariant holds during event ingest"
    - "Container build/vet/test all green"
  artifacts: []
  key_links: []
---

<objective>
Implement event ingest from L2 and validate single-router invariant.

Purpose: Go must ingest runtime events for observability without accidentally triggering rotation. This is the most critical behavioral invariant.
Output: Event ingest + regression test + GATE P3.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/03-integracao/03-01-SUMMARY.md
@Diligencias/03_INTEGRACAO_P3.md
@docs/contract/single-router-invariant.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement RuntimeEventStream ingest</name>
  <files>server/internal/l2runtime/client.go, server/internal/daemon/daemon.go</files>
  <read_first>server/internal/l2runtime/client.go, server/internal/daemon/daemon.go, docs/contract/rpp-l2-v1-event-schema.json</read_first>
  <action>
    1. L2 client receives RuntimeEventStream from prodex runtime
    2. Events parsed per rpp.l2.v1 schema
    3. Events forwarded to observability/ledger subsystem in Go
    4. CRITICAL: event handler MUST NOT call any rotation/routing function
    5. Validate schema compliance for each ingested event
  </action>
  <verify>go test ./internal/l2runtime/... -run TestEventIngest -count=1</verify>
  <done>Event ingest operational without rotation side-effects</done>
</task>

<task type="auto">
  <name>Task 2: Single-router regression test + GATE P3</name>
  <files>server/internal/daemon/daemon_test.go, server/internal/l2runtime/client_test.go</files>
  <read_first>server/internal/daemon/daemon_test.go, docs/contract/single-router-invariant.md</read_first>
  <action>
    1. Create test: inject runtime events during active session → verify Go's rotate() is NOT called
    2. Create test: verify rotation only happens at session boundary (rotate-before-commit)
    3. Run full container gate:
       docker run --rm --sysctl net.ipv6.conf.all.disable_ipv6=1 \
         -v .../multica-auth-work:/src -v multica-gomod:/go/pkg/mod \
         -w /src/server golang:1.26-alpine sh -c \
         'apk add --no-cache git; export HOME=/tmp/gohome; mkdir -p $HOME; \
          go build ./... && go vet ./internal/... && \
          go test ./internal/daemon ./internal/l2runtime -count=1'

    GATE P3: build/vet/test green + rotation test passes
  </action>
  <verify>echo "GATE P3: build/vet/test green, ingest-no-rotation test passes"</verify>
  <done>GATE P3 closed — integration complete</done>
</task>

</tasks>

<verification>
- [ ] Event ingest works end-to-end
- [ ] Rotation regression test passes
- [ ] Container build/vet/test all green
- [ ] GATE P3 complete
</verification>

<output>After completion, create `.planning/phases/03-integracao/03-02-SUMMARY.md`</output>
