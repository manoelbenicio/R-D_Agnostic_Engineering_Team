---
phase: 03-integracao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/internal/daemon/prodex.go, server/internal/daemon/l2_runtime.go, server/internal/l2runtime/client.go]
autonomous: true
requirements: [REQ-05]
user_setup: []
must_haves:
  truths:
    - "Sidecar prodex starts, stops, and reports health via daemon"
    - "ApplyPolicy pushes policy/budgets/kill-switch from Go to L2"
    - "RegisterAccounts pushes approved accounts from Go to L2"
  artifacts:
    - path: "server/internal/daemon/l2_runtime.go"
      provides: "Sidecar lifecycle management"
    - path: "server/internal/l2runtime/client.go"
      provides: "L2 runtime client for contract operations"
  key_links:
    - from: "server/internal/daemon/prodex.go"
      to: "server/internal/l2runtime/client.go"
      via: "Daemon uses L2 client for lifecycle"
---

<objective>
Implement sidecar lifecycle and policy push from Go to L2 runtime.

Purpose: Make Multica Go actually launch, manage, and push desired-state to the prodex runtime. This is the bridge from contract (P1) to running integration.
Output: Working sidecar lifecycle + policy push in daemon.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Diligencias/03_INTEGRACAO_P3.md
@docs/contract/rpp-l2-v1-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sidecar lifecycle (start/stop/health)</name>
  <files>server/internal/daemon/l2_runtime.go, server/internal/daemon/prodex.go</files>
  <read_first>server/internal/daemon/prodex.go, server/internal/daemon/daemon.go, server/internal/l2runtime/client.go, Diligencias/03_INTEGRACAO_P3.md</read_first>
  <action>
    On FLEET HOST — modify Go code:

    1. Sidecar start: daemon launches prodex process using resolved binary path
    2. Health check: periodic HealthCheck call to L2 runtime
    3. Graceful stop: StopSession for active sessions, then process termination
    4. Restart on crash: automatic restart with exponential backoff
    5. Kill-switch: immediately disable routing per tenant/provider/profile

    HOTSPOT: server/internal/daemon/* is serial — coordinate before editing.
  </action>
  <verify>go test ./internal/daemon/... -count=1</verify>
  <acceptance_criteria>
    - "l2_runtime.go implements Start(), Stop(), Health() methods"
    - "Daemon launches prodex process on startup when MULTICA_PRODEX_ENABLED=1"
    - "Health check runs periodically"
  </acceptance_criteria>
  <done>Sidecar lifecycle operational</done>
</task>

<task type="auto">
  <name>Task 2: Implement policy push (ApplyPolicy + RegisterAccounts)</name>
  <files>server/internal/l2runtime/client.go</files>
  <read_first>server/internal/l2runtime/client.go, docs/contract/rpp-l2-v1-contract.md</read_first>
  <action>
    On FLEET HOST:

    1. ApplyPolicy: Go pushes policy/budgets/kill-switch configuration to L2
    2. RegisterAccounts: Go pushes approved accounts list to L2
    3. Both operations follow the contract spec from rpp.l2.v1
    4. Error handling: fail-closed on communication errors
    5. Idempotent: repeated pushes are safe
  </action>
  <verify>go test ./internal/l2runtime/... -count=1</verify>
  <acceptance_criteria>
    - "client.go implements ApplyPolicy() and RegisterAccounts()"
    - "Both methods follow rpp.l2.v1 contract"
    - "Error handling is fail-closed"
  </acceptance_criteria>
  <done>Policy push implemented per contract</done>
</task>

</tasks>

<verification>
- [ ] go build ./... green
- [ ] go test ./internal/daemon ./internal/l2runtime green
- [ ] Sidecar lifecycle works (start/stop/health)
- [ ] Policy push follows contract
</verification>

<output>After completion, create `.planning/phases/03-integracao/03-01-SUMMARY.md`</output>
