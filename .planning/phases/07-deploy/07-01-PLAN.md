---
phase: 07-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - multica-auth-work/prodex-sidecar/src/main.rs
  - scripts/smoke/p7-kill-switch-exercise.sh
  - scripts/deploy/rollback-to-raw-codex.sh
  - scripts/smoke/rollback-smoke.sh
  - .deploy-control/evidence/p7-kill-switch-test.md
  - .deploy-control/evidence/p7-rollback-test.md
  - .planning/phases/07-deploy/07-01-SUMMARY.md
autonomous: true
requirements: [REQ-19, REQ-20]
user_setup: []
must_haves:
  truths:
    - "Kill-switch works per tenant/provider/profile — tested by real exercise"
    - "One-command rollback to raw codex works — tested and verified"
    - "Kill-switch disables Smart Context, gateway, auto-redeem"
  artifacts:
    - path: ".deploy-control/evidence/p7-kill-switch-test.md"
      provides: "Kill-switch test evidence"
    - path: ".deploy-control/evidence/p7-rollback-test.md"
      provides: "Rollback test evidence"
  key_links: []
---

<objective>
Test kill-switch and one-command rollback before any PROD deploy.

Purpose: Deploy is blocked until both kill-switch and rollback are proven by REAL test, not documentation. These are the safety nets for direct-to-PROD deploy.
Output: Evidence of both tests passing.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@openspec/changes/rotation-parity-polyglot/specs/deploy-rollback/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill-switch exercise per tenant/provider/profile</name>
  <action>
    1. Activate kill-switch for a specific tenant — verify routing stops
    2. Activate kill-switch for a specific provider — verify provider disabled
    3. Activate kill-switch for a specific profile — verify profile blocked
    4. Verify: Smart Context disabled when kill-switch active
    5. Verify: gateway disabled when kill-switch active
    6. Verify: auto-redeem disabled when kill-switch active
    7. Deactivate kill-switch — verify routing resumes
    8. Record ALL evidence with timestamps
  </action>
  <done>Kill-switch proven for tenant, provider, and profile levels</done>
</task>

<task type="auto">
  <name>Task 2: One-command rollback to raw codex</name>
  <action>
    1. With prodex active, execute single rollback command
    2. Verify: system falls back to raw codex behavior
    3. Verify: no prodex routing occurs after rollback
    4. Verify: prior behavior (pre-prodex) is restored
    5. Roll forward: re-enable prodex, verify it works again
    6. Record evidence of both directions
  </action>
  <done>Rollback proven in both directions</done>
</task>

</tasks>

<verification>
- [x] Kill-switch tested per tenant/provider/profile
- [x] Rollback to raw codex tested
- [x] Roll-forward back to prodex tested
- [x] All evidence scrubbed and recorded
</verification>

<output>After completion, create `.planning/phases/07-deploy/07-01-SUMMARY.md`</output>
