---
phase: 07-deploy
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [docs/deploy/runbook.md, .github/workflows/ci.yml]
autonomous: false
requirements: [REQ-21, REQ-25, REQ-38, REQ-39]
user_setup: []
must_haves:
  truths:
    - "Logs are scrubbed — no secret in PROD path"
    - "Deploy runbook references Helm + docker-compose.selfhost + observability"
    - "CI includes go vet + lint + security scan beyond go test"
    - "Deploy is direct-to-PROD only after ALL prerequisites green"
  artifacts:
    - path: "docs/deploy/runbook.md"
      provides: "Complete deploy runbook"
    - path: "docs/deploy/observability.md"
      provides: "Monitoring and alerting setup"
  key_links: []
---

<objective>
Deploy to PROD with full safety net: scrubbed logs, runbook, CI hardening.

Purpose: Direct-to-PROD deploy (no canary/staging) is mitigated by kill-switch + rollback (proven in 07-01) + exhaustive QA (P6) + scrubbed logs.
Output: Deploy executed + runbook + CI hardened.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/07-deploy/07-01-SUMMARY.md
@openspec/changes/rotation-parity-polyglot/specs/deploy-rollback/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify log scrubbing in PROD path</name>
  <action>
    1. Scan all log/trace/error/audit output paths
    2. Verify no secret/token/key appears (redaction verified)
    3. Test with injected fake credentials — must be masked
    4. Record evidence in .deploy-control/evidence/p7-log-scrubbing.md
  </action>
  <done>No secrets in PROD path logs</done>
</task>

<task type="auto">
  <name>Task 2: Create deploy runbook + CI hardening</name>
  <files>docs/deploy/runbook.md, .github/workflows/ci.yml</files>
  <read_first>deploy/helm/, deploy/observability/, docker-compose.selfhost.yml</read_first>
  <action>
    Runbook (docs/deploy/runbook.md):
    1. Reference Helm charts (deploy/helm/)
    2. Reference docker-compose.selfhost for self-hosted deploy
    3. Reference deploy/observability for monitoring
    4. Include reversible migrations (322 .sql) as deploy step
    5. Include kill-switch activation/deactivation procedure
    6. Include rollback procedure

    CI hardening (.github/workflows/ci.yml):
    1. Add go vet to existing CI
    2. Add golangci-lint
    3. Add govulncheck (security scan)
    4. Add gitleaks (secret scanning)
  </action>
  <done>Runbook complete, CI hardened</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full deploy pipeline: kill-switch tested, rollback tested, QA green, logs scrubbed, runbook ready</what-built>
  <how-to-verify>Review: kill-switch evidence, rollback evidence, QA C1-C6 evidence, log scrubbing evidence, runbook completeness. Confirm PROD deploy authorization.</how-to-verify>
  <resume-signal>Type "deploy-approved" to proceed with PROD deploy, or describe concerns</resume-signal>
</task>

</tasks>

<verification>
- [ ] Logs scrubbed (no secrets in PROD path)
- [ ] Runbook complete (Helm + compose + migrations)
- [ ] CI hardened (vet + lint + security)
- [ ] Human approval for PROD deploy
- [ ] GATE P7: deploy direct to PROD
</verification>

<output>After completion, create `.planning/phases/07-deploy/07-02-SUMMARY.md`</output>
