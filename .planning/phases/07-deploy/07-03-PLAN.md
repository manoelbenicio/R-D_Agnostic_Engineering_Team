---
phase: 07-deploy
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - docs/deploy/prodex-account-enrollment-runbook.md
  - docs/deploy/prod-rollout-runbook.md
  - openspec/changes/rotation-parity-polyglot/tasks.md
autonomous: true
requirements: [REQ-19, REQ-20, REQ-21, REQ-25, REQ-39]
user_setup:
  - "Free host port 3000 before production observability deploy; this local machine has a WSL relay serving /login on localhost:3000."
must_haves:
  truths:
    - "Observability compose is syntactically valid and the four services can run locally."
    - "Enrollment has an operator runbook that keeps raw credentials out of logs and evidence."
    - "PROD rollout runbook references Helm, docker-compose.selfhost, observability, and reversible migrations."
  artifacts:
    - path: "docs/deploy/prodex-account-enrollment-runbook.md"
      provides: "prodex profile/account enrollment procedure"
    - path: "docs/deploy/prod-rollout-runbook.md"
      provides: "deployment surface references and migration gate"
    - path: ".deploy-control/evidence/p7-observability-enrollment-helm-20260705.md"
      provides: "P7 validation evidence"
  key_links:
    - "multica-auth-work/deploy/observability/docker-compose.yml"
    - "multica-auth-work/docker-compose.selfhost.yml"
    - "multica-auth-work/deploy/helm/multica"
---

<objective>
Close P7 tasks 7.4a, 7.4e, and 7.6 for observability, prodex account enrollment, and deploy runbook coverage.

Output: observability stack validation, enrollment runbook, rollout runbook references for Helm/selfhost/observability/migrations, and OpenSpec task updates.
</objective>

<context>
@openspec/changes/rotation-parity-polyglot/proposal.md
@openspec/changes/rotation-parity-polyglot/design.md
@openspec/changes/rotation-parity-polyglot/tasks.md
@docs/deploy/prod-rollout-runbook.md
@multica-auth-work/deploy/observability/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Observability stack validation</name>
  <action>
    1. Validate `deploy/observability/docker-compose.yml` with `docker compose config`.
    2. Validate Prometheus config and alert rules with `promtool`.
    3. Validate Alertmanager config with `amtool`.
    4. Validate Grafana dashboard JSON files.
    5. Start Prometheus, Alertmanager, postgres-exporter, and Grafana.
    6. Confirm readiness/health endpoints.
  </action>
  <done>
    Local stack is running. Prometheus `9090`, Alertmanager `9093`, and postgres-exporter `9187` are published on expected ports. Grafana container serves port `3000`, but local host port `3000` was already occupied by `wslrelay`; for this validation Grafana is published as `127.0.0.1:13000->3000`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrollment runbook</name>
  <action>
    Create `docs/deploy/prodex-account-enrollment-runbook.md` covering gates, POSIX filesystem validation, supported vendors, idempotent enrollment command, post-enrollment SQL checks, failure handling, and evidence fields.
  </action>
  <done>Runbook created with no raw secret capture path.</done>
</task>

<task type="auto">
  <name>Task 3: Helm/selfhost/observability/migration references</name>
  <action>
    Update `docs/deploy/prod-rollout-runbook.md` with deployment surfaces for Helm, self-host Docker Compose, observability compose, reversible migration check, and account enrollment reference.
  </action>
  <done>Runbook now references `deploy/helm`, `docker-compose.selfhost.yml`, `deploy/observability`, and migration up/down parity.</done>
</task>

</tasks>

<verification>
- [x] `docker compose config` passes for observability.
- [x] Prometheus config and 10 alert rules pass `promtool`.
- [x] Alertmanager config passes `amtool`.
- [x] 6 Grafana dashboard JSON files parse.
- [x] 4 observability containers running locally.
- [x] Grafana health API returns `database: ok`.
- [x] Migration parity is 161 up / 161 down with no unmatched stems.
- [x] Enrollment runbook exists.
- [x] PROD rollout runbook references Helm, selfhost compose, observability, reversible migrations, and enrollment.
</verification>

<notes>
Production must free host port `3000` before running the observability compose exactly as written. This workstation already had `wslrelay.exe` listening on `localhost:3000` and returning `/login`, so Grafana was validated on host port `13000` while preserving container port `3000`.
</notes>
