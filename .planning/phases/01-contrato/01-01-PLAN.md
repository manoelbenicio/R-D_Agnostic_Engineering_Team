---
phase: 01-contrato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/contract/rpp-l2-v1-contract.md, docs/contract/rpp-l2-v1-event-schema.json]
autonomous: true
requirements: [REQ-04]
user_setup: []
must_haves:
  truths:
    - "Contract defines all 8 operations: HealthCheck, ApplyPolicy, RegisterAccounts, StartSession, StopSession, RouteDecisionEvent, RuntimeEventStream, KillSwitch"
    - "Event schema validates against JSON Schema Draft 2020-12"
    - "Schema is versioned as rpp.l2.v1"
    - "MCP tool-call events are covered in RuntimeEventStream"
    - "tenant_id/session_id are conditional (required only in session-scoped events)"
  artifacts:
    - path: "docs/contract/rpp-l2-v1-contract.md"
      provides: "Versioned contract specification"
      contains: "rpp.l2.v1"
    - path: "docs/contract/rpp-l2-v1-event-schema.json"
      provides: "JSON Schema for runtime events"
      contains: "$schema"
  key_links:
    - from: "docs/contract/rpp-l2-v1-contract.md"
      to: "docs/contract/rpp-l2-v1-event-schema.json"
      via: "Contract references schema for event validation"
---

<objective>
Define the versioned Go↔L2 runtime contract and event schema.

Purpose: This contract is the formal interface between Multica Go (control plane) and prodex/Rust (runtime L2). Without it, integration (P3) cannot proceed with clear boundaries.
Output: Contract document with 8 operations + JSON Schema for events.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Diligencias/01_CONTRATO_P1.md
@openspec/changes/rotation-parity-polyglot/specs/l2-runtime-contract/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define contract operations (rpp.l2.v1)</name>
  <files>docs/contract/rpp-l2-v1-contract.md</files>
  <read_first>Diligencias/01_CONTRATO_P1.md, openspec/changes/rotation-parity-polyglot/specs/l2-runtime-contract/spec.md, server/internal/l2runtime/client.go</read_first>
  <action>
    Create docs/contract/rpp-l2-v1-contract.md defining:

    1. HealthCheck — L2 runtime health probe
    2. ApplyPolicy — Go pushes policy/budgets/kill-switch to L2
    3. RegisterAccounts — Go pushes approved accounts to L2
    4. StartSession — Go signals session start to L2
    5. StopSession — Go signals session end to L2
    6. RouteDecisionEvent — L2 reports routing decisions back to Go
    7. RuntimeEventStream — L2 streams runtime events (including MCP tool-call events, session affinity, rotation decisions)
    8. KillSwitch — Go can disable L2 routing per tenant/provider/profile

    For each operation: direction (Go→L2 or L2→Go), payload schema reference, error handling, idempotency guarantees.

    MCP coverage: RuntimeEventStream MUST surface MCP tool events. Session affinity MUST preserve tool_call/continuation state across rotation.
  </action>
  <verify>grep -c "HealthCheck\|ApplyPolicy\|RegisterAccounts\|StartSession\|StopSession\|RouteDecisionEvent\|RuntimeEventStream\|KillSwitch" docs/contract/rpp-l2-v1-contract.md</verify>
  <acceptance_criteria>
    - "Contract document defines all 8 operations"
    - "Each operation has direction, payload, error handling"
    - "MCP tool-call events are explicitly covered"
    - "Document is versioned as rpp.l2.v1"
  </acceptance_criteria>
  <done>Contract specification complete with 8 operations</done>
</task>

<task type="auto">
  <name>Task 2: Create JSON Schema for runtime events</name>
  <files>docs/contract/rpp-l2-v1-event-schema.json</files>
  <read_first>Diligencias/01_CONTRATO_P1.md</read_first>
  <action>
    Create docs/contract/rpp-l2-v1-event-schema.json:

    1. Use JSON Schema Draft 2020-12
    2. Version: rpp.l2.v1
    3. Event types: sidecar_started, session_started, session_stopped, route_decision, policy_applied, health_status, mcp_tool_call, mcp_tool_result, kill_switch_activated
    4. IMPORTANT: tenant_id and session_id are CONDITIONAL:
       - Required in session-scoped events (session_started, route_decision, mcp_tool_call)
       - NOT required in global events (sidecar_started, health_status)
       - Implement via allOf if/then pattern
    5. Create positive/negative fixture examples for validation
  </action>
  <verify>python3 -c "import json; json.load(open('docs/contract/rpp-l2-v1-event-schema.json'))" 2>/dev/null || echo "Valid JSON"</verify>
  <acceptance_criteria>
    - "Schema uses $schema Draft 2020-12"
    - "Schema version is rpp.l2.v1"
    - "tenant_id/session_id are conditional via allOf if/then"
    - "MCP tool events (mcp_tool_call, mcp_tool_result) are defined"
  </acceptance_criteria>
  <done>Event schema compiles and validates correctly</done>
</task>

</tasks>

<verification>
- [ ] Contract document covers all 8 operations
- [ ] Schema is valid JSON Schema Draft 2020-12
- [ ] MCP tool events covered
- [ ] Conditional fields implemented correctly
- [ ] No secrets in any document
</verification>

<success_criteria>
- Contract ready for P3 integration implementation
- Schema can be used for runtime event validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-contrato/01-01-SUMMARY.md`
</output>
