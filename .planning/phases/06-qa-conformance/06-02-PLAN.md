---
phase: 06-qa-conformance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-16, REQ-17, REQ-18, REQ-26]
user_setup: []
must_haves:
  truths:
    - "C5: Smart Context measured shadow→canary→live with automatic exact fallback"
    - "C6: triple CODEX_HOME × prodex × Herdr coexists without auth clobber"
    - "Herdr coordination smoke passes (agent send/notification/events)"
    - "MCP passthrough conformance proven across providers"
  artifacts:
    - path: ".deploy-control/evidence/c5-smart-context.md"
      provides: "C5 Smart Context evidence"
    - path: ".deploy-control/evidence/c6-isolation.md"
      provides: "C6 triple isolation evidence"
    - path: ".deploy-control/evidence/herdr-smoke.md"
      provides: "Herdr coordination evidence"
    - path: ".deploy-control/evidence/mcp-conformance.md"
      provides: "MCP passthrough evidence"
  key_links: []
---

<objective>
Execute conformance gates C5–C6, Herdr smoke, and MCP conformance.

Purpose: Smart Context with fallback, triple isolation, and Herdr coordination are critical production requirements.
Output: Scrubbed evidence files for C5, C6, Herdr, and MCP.
</objective>

<context>
@.planning/PROJECT.md
@Diligencias/06_QA_CONFORMANCE_P6.md
@openspec/changes/rotation-parity-polyglot/specs/qa-conformance/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: C5 — Smart Context shadow→canary→live</name>
  <action>
    1. Deploy Smart Context in shadow mode: measure before/after metrics
    2. Promote to canary: validate metrics within tolerance
    3. If structural/protocol risk detected: automatic exact fallback
    4. Measure: token savings, context quality, response coherence
    5. Record metrics + fallback behavior in evidence
  </action>
  <done>C5 green with metrics and fallback proven</done>
</task>

<task type="auto">
  <name>Task 2: C6 — Triple isolation (CODEX_HOME × prodex × Herdr)</name>
  <action>
    1. Run with 3 concurrent contexts: CODEX_HOME isolated, prodex with PRODEX_HOME, Herdr
    2. Verify: no auth clobber between accounts (AccountHome mandatory)
    3. Verify: profile isolation holds across all 3
    4. Verify: each context has its own credentials, no leakage
    5. Record isolation proof in evidence
  </action>
  <done>C6 green — triple isolation without clobber</done>
</task>

<task type="auto">
  <name>Task 3: Herdr smoke + MCP conformance + GATE P6</name>
  <action>
    Herdr smoke:
    1. agent send: send message between agents via herdr
    2. notification: verify notification delivery
    3. events: verify event stream works

    MCP conformance:
    1. Passthrough: MCP tool-calls pass through prodex correctly
    2. Translation: tool-calls translate across providers (anthropic→gemini etc.)
    3. Affinity: tool_call/continuation state preserved across rotation

    GATE P6: ALL C1–C6 green with scrubbed evidence
    - Reclassify any plan-only/dry-run as IN_PROGRESS
    - Validator re-runs checks (don't trust the tail)
  </action>
  <done>GATE P6 closed — all conformance gates green</done>
</task>

</tasks>

<verification>
- [ ] C5 Smart Context with fallback green
- [ ] C6 triple isolation green
- [ ] Herdr coordination smoke green
- [ ] MCP conformance green
- [ ] ALL C1–C6 green with scrubbed evidence
- [ ] No plan-only/dry-run marked DONE
</verification>

<output>After completion, create `.planning/phases/06-qa-conformance/06-02-SUMMARY.md`</output>
