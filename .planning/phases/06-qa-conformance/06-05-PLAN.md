# Phase 06-QA-Conformance: Plan 06-05

## Context
This plan addresses two critical QA Conformance tasks from the `runtime-conformance-plan.md` matrix:
- **6.3 C3 Replay**: Test continuation affinity over compact, SSE, and WebSocket replays.
- **6.4 C4 Troca de perfil fail-closed**: Prove that requesting a missing/invalid profile fails closed before the first commit.

*Note: Live PROD validation is F0-GATED. We will provide unit/contract test evidence (dry-run) to satisfy the pre-F0 acceptance criteria.*

## Execution Steps

### 1. Implement Unit/Contract Tests (Dry-Run Evidence)
Create a conformance test file in the Go daemon `multica-auth-work/server/internal/daemon/qa_conformance_test.go` to assert:
- **C3**: When `previous_response_id` is present, the test mock simulates hard affinity. The test must assert that Go does not override the L2 owner's affinity decision.
- **C4**: Simulating a switch to a missing profile (`profile B`) while `profile A` is active must result in a `profile_switch_fail_closed` error, avoiding silent reuse of `profile A`.

### 2. Validate Tests in Container
Execute the tests locally within the isolated `golang:latest` Docker container (with IPv6 disabled) to generate clean dry-run evidence.

### 3. Record Evidence and Mark Done
- Create `.planning/phases/06-qa-conformance/06-05-SUMMARY.md` documenting the results.
- Update `openspec/changes/rotation-parity-polyglot/tasks.md` to mark tasks 6.3 and 6.4 as done (`[x]`).
- Check out of Phase P6 in `.deploy-control`.
