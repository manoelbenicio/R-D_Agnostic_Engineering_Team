---
phase: 06-qa-conformance
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - .deploy-control/evidence/c5-smart-context-live.md
  - .deploy-control/evidence/c6-isolation-live.md
  - .planning/phases/06-qa-conformance/06-07-SUMMARY.md
  - openspec/changes/rotation-parity-polyglot/tasks.md
autonomous: true
requirements: [REQ-16, REQ-17]
user_setup: []
must_haves:
  truths:
    - "C5 Smart Context shadow→canary→live QA flow exercised against the real sidecar"
    - "C5 sidecar policy/session/event flow is bearer-authenticated and scrubbed"
    - "C6 CODEX_HOME x PRODEX_HOME x Herdr isolation exercised with fake credentials only"
    - "C6 account registration exercised against the real sidecar"
  artifacts:
    - path: ".deploy-control/evidence/c5-smart-context-live.md"
      provides: "C5 live sidecar QA evidence"
    - path: ".deploy-control/evidence/c6-isolation-live.md"
      provides: "C6 live sidecar and synthetic isolation evidence"
---

# PLAN 06-07 — P6 C5/C6 QA Takeover

## Objective

Execute tasks 6.5 and 6.6 from `openspec/changes/rotation-parity-polyglot/tasks.md` against the real `prodex-sidecar` binary without touching Rust.

## Task 1 — C5 Smart Context Shadow→Canary→Live

1. Start `multica-auth-work/prodex-sidecar/target/release/prodex-sidecar 127.0.0.1:43117`.
2. Probe `http://127.0.0.1:43117/readyz` with bearer auth.
3. Exercise policy transitions:
   - shadow: `smart_context.mode=shadow`, `canary_percent=0`;
   - canary: `smart_context.mode=canary`, `canary_percent=1`;
   - live: `smart_context.mode=live`, with fallback probes still forcing exact pass-through on risky inputs.
4. Validate scrubbed runtime events and session lifecycle from the real sidecar.
5. Record that no mock sidecar was used for final evidence.

## Task 2 — C6 CODEX_HOME × prodex × Herdr Isolation

1. Create temporary fake credential homes only; do not read real OAuth material.
2. Exercise `/v1/accounts/register` on the real sidecar:
   - accept profiles under a temp `PRODEX_HOME`;
   - reject raw auth payloads;
   - reject profile homes outside the managed temp root.
3. Run a synthetic filesystem isolation probe:
   - fake `CODEX_HOME`;
   - fake `PRODEX_HOME/profiles/<profile>`;
   - fake Herdr pane env.
4. Prove task/profile refresh touches only the intended fake path.
5. Record live sidecar response plus synthetic filesystem no-clobber proof.

## Verification

- [x] C5 live shadow/canary/live desired-state transitions pass.
- [x] C5 live session/event flow is scrubbed and passes.
- [x] C6 live account registration rejects invalid homes.
- [x] C6 synthetic filesystem isolation passes.
- [x] No Rust files modified.
- [x] `tasks.md` marks 6.5 and 6.6 complete for this live deliverable.

## Output

Create `.planning/phases/06-qa-conformance/06-07-SUMMARY.md`.
