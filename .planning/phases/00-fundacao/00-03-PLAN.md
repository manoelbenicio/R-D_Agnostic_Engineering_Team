---
phase: 00-fundacao
plan: 03
type: execute
wave: 2
depends_on: ["00-01"]
files_modified: []
autonomous: true
requirements: [REQ-02, REQ-03]
user_setup: []
must_haves:
  truths:
    - "Postgres :5432 is reachable from server container"
    - "Redis :6379 is reachable from server container"
    - "go build ./... succeeds in container"
    - "go vet ./internal/... succeeds in container"
    - "go test passes for daemon and l2runtime packages"
    - "prodex subcommands inventory documented"
  artifacts:
    - path: ".deploy-control/evidence/p0-datastores.md"
      provides: "Datastore reachability evidence"
    - path: ".deploy-control/evidence/p0-server-build.md"
      provides: "Server build/vet/test evidence"
    - path: "docs/prodex/subcommand-inventory.md"
      provides: "Prodex subcommand usage map"
  key_links: []
---

<objective>
Validate datastores, server build gate, and inventory prodex subcommands.

Purpose: Confirm the full development environment is ready — Postgres/Redis reachable, Go server compiles and tests pass, and prodex CLI surface is documented.
Output: Evidence files for datastores, server build, and subcommand inventory.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Diligencias/00_FUNDACAO_P0.md
@Diligencias/00d_CONFIG_ENV_SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Postgres and Redis reachability</name>
  <files>.deploy-control/evidence/p0-datastores.md</files>
  <read_first>Diligencias/00_FUNDACAO_P0.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    1. Check Postgres:
       docker exec deploy-postgres-1 pg_isready -U postgres
       # Expected: accepting connections on port 5432

    2. Check Redis:
       docker exec deploy-redis-1 redis-cli ping
       # Expected: PONG

    3. Record evidence:
       mkdir -p .deploy-control/evidence
       Record output in .deploy-control/evidence/p0-datastores.md
  </action>
  <verify>docker exec deploy-postgres-1 pg_isready && docker exec deploy-redis-1 redis-cli ping</verify>
  <acceptance_criteria>
    - "Postgres responds 'accepting connections' on port 5432"
    - "Redis responds PONG"
  </acceptance_criteria>
  <done>Both datastores reachable and healthy</done>
</task>

<task type="auto">
  <name>Task 2: Validate server Go build/vet/test in container</name>
  <files>.deploy-control/evidence/p0-server-build.md</files>
  <read_first>Diligencias/00b_DEPENDENCY_SOURCES.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    docker run --rm --sysctl net.ipv6.conf.all.disable_ipv6=1 \
      -v /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/multica-auth-work:/src \
      -v multica-gomod:/go/pkg/mod \
      -w /src/server golang:1.26-alpine sh -c \
      'apk add --no-cache git >/dev/null; export HOME=/tmp/gohome; mkdir -p $HOME; \
       echo "=== BUILD ===" && go build ./... && \
       echo "=== VET ===" && go vet ./internal/... && \
       echo "=== TEST ===" && go test ./internal/daemon ./internal/l2runtime -count=1'

    Record output in .deploy-control/evidence/p0-server-build.md
    Expected: build=0, vet=0, 24/24 test packages OK
  </action>
  <verify>echo "Build, vet, and test all returned exit 0"</verify>
  <acceptance_criteria>
    - "go build ./... exits 0"
    - "go vet ./internal/... exits 0"
    - "go test for daemon and l2runtime exits 0 with 0 failures"
  </acceptance_criteria>
  <done>Server build gate green in container</done>
</task>

<task type="auto">
  <name>Task 3: Inventory prodex subcommands used by Multica</name>
  <files>docs/prodex/subcommand-inventory.md</files>
  <read_first>Diligencias/00d_CONFIG_ENV_SECURITY.md, server/internal/daemon/prodex.go</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    1. Document all prodex subcommands from source inspection:
       - run / s (launch) — used by F3 daemon
       - redeem (reset-claim) — used by F9
       - mcp (MCP server) — used by P1/P6
       - auth / login (credential flow)
       - doctor / super-doctor (health/diagnostics)
       - profile (isolation)
       - quota (quota management)
       - status (runtime status)
       - compact-output (Smart Context)
       - replay-report (QA replay)

    2. Create docs/prodex/subcommand-inventory.md with:
       - Subcommand name
       - Which Multica module uses it
       - Phase coverage (P0-P9)
       - Status: used / mapped / to-decide

    3. Cross-reference with grep of server/ for prodex invocations.
  </action>
  <verify>test -f docs/prodex/subcommand-inventory.md</verify>
  <acceptance_criteria>
    - "subcommand-inventory.md lists all 11+ subcommands"
    - "Each subcommand has Multica usage context"
    - "run/s/redeem/mcp are marked as 'used'"
  </acceptance_criteria>
  <done>Subcommand inventory complete and documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Postgres :5432 reachable
- [ ] Redis :6379 reachable
- [ ] go build/vet/test all green in container
- [ ] Subcommand inventory documented
- [ ] Evidence files created in .deploy-control/evidence/
</verification>

<success_criteria>
- Development environment fully validated
- Server compiles and passes tests
- Prodex CLI surface documented
- GATE P0 ready to close (combined with 00-01 and 00-02 results)
</success_criteria>

<output>
After completion, create `.planning/phases/00-fundacao/00-03-SUMMARY.md`
</output>
