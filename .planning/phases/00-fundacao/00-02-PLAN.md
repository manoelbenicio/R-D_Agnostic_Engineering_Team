---
phase: 00-fundacao
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified: [server/internal/daemon/prodex.go, server/internal/daemon/config.go]
autonomous: true
requirements: [REQ-02]
user_setup: []
must_haves:
  truths:
    - "MULTICA_PRODEX_ENABLED=1 is set in server environment"
    - "MULTICA_PRODEX_PATH points to the built binary"
    - "MULTICA_PRODEX_VERSION=v0.246.0 and MULTICA_PRODEX_COMMIT=7750da9b are set"
    - "exec.LookPath resolves the prodex binary"
    - "Multica startup fails-closed if VERSION or COMMIT env vars are missing"
  artifacts:
    - path: "server/internal/daemon/prodex.go"
      provides: "Prodex integration with exec.LookPath resolution"
      contains: "exec.LookPath"
    - path: "server/internal/daemon/config.go"
      provides: "Probe config for prodex CLI"
      contains: "MULTICA_PRODEX"
  key_links:
    - from: "server/internal/daemon/config.go"
      to: "server/internal/daemon/prodex.go"
      via: "env var resolution for prodex path"
      pattern: "MULTICA_PRODEX_PATH"
---

<objective>
Wire prodex binary into Multica environment and confirm executable resolution.

Purpose: Multica Go must be able to find and launch the prodex binary via exec.LookPath. Without proper env vars, the daemon fails-closed (by design).
Output: Environment configured, exec.LookPath test passes.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-fundacao/00-01-SUMMARY.md
@Diligencias/00_FUNDACAO_P0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Multica environment variables</name>
  <files>server/.env, server/internal/daemon/config.go</files>
  <read_first>server/internal/daemon/config.go, server/internal/daemon/prodex.go, Diligencias/00_FUNDACAO_P0.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    1. Set environment variables (in server .env or daemon config, NOT in logs):
       MULTICA_PRODEX_ENABLED=1
       MULTICA_PRODEX_PATH="$HOME/runtime/prodex-src/target/release/prodex"
       MULTICA_PRODEX_VERSION=v0.246.0
       MULTICA_PRODEX_COMMIT=7750da9b
       PRODEX_HOME="$HOME/runtime/prodex-home"

    2. Create PRODEX_HOME directory:
       mkdir -p "$HOME/runtime/prodex-home"

    3. Verify config.go probe pattern:
       Read server/internal/daemon/config.go — confirm probe() function
       resolves via envVar → defaultCmd → modelEnv pattern.
       The lookPath variable in daemon.go must resolve the binary path.

    NOTE: prodex.go requires VERSION AND COMMIT together — if either is
    missing, it fails-closed. This is correct behavior (do not change).
  </action>
  <verify>grep -q "MULTICA_PRODEX_ENABLED" server/.env 2>/dev/null || echo "Check env injection method"</verify>
  <acceptance_criteria>
    - "MULTICA_PRODEX_ENABLED=1 is set"
    - "MULTICA_PRODEX_PATH points to existing executable"
    - "MULTICA_PRODEX_VERSION and MULTICA_PRODEX_COMMIT are both set"
    - "PRODEX_HOME directory exists at ~/runtime/prodex-home"
  </acceptance_criteria>
  <done>All 5 env vars set and PRODEX_HOME created</done>
</task>

<task type="auto">
  <name>Task 2: Verify exec.LookPath resolution</name>
  <files>server/internal/daemon/prodex.go</files>
  <read_first>server/internal/daemon/prodex.go, server/internal/daemon/daemon.go</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    1. Run the Go test that covers prodex resolution:
       docker run --rm --sysctl net.ipv6.conf.all.disable_ipv6=1 \
         -v /mnt/c/VMs/Projects/RD_Agnostic_Engineering_Team/multica-auth-work:/src \
         -v multica-gomod:/go/pkg/mod \
         -e MULTICA_PRODEX_ENABLED=1 \
         -e MULTICA_PRODEX_PATH="$HOME/runtime/prodex-src/target/release/prodex" \
         -e MULTICA_PRODEX_VERSION=v0.246.0 \
         -e MULTICA_PRODEX_COMMIT=7750da9b \
         -w /src/server golang:1.26-alpine sh -c \
         'apk add --no-cache git >/dev/null; export HOME=/tmp/gohome; mkdir -p $HOME; \
          go test ./internal/daemon/... -run TestProdex -count=1 -v'

    2. Confirm exec.LookPath resolves the binary path.
       If the test does not exist, verify manually:
       which prodex || echo "$MULTICA_PRODEX_PATH" && test -x "$MULTICA_PRODEX_PATH"
  </action>
  <verify>test -x "$MULTICA_PRODEX_PATH"</verify>
  <acceptance_criteria>
    - "exec.LookPath or direct path resolves to the built binary"
    - "Binary is executable from the daemon's perspective"
  </acceptance_criteria>
  <done>Multica can resolve and would launch the prodex binary</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 env vars set (ENABLED, PATH, VERSION, COMMIT, PRODEX_HOME)
- [ ] PRODEX_HOME directory exists
- [ ] exec.LookPath resolves the binary
- [ ] No secrets in any log or evidence file
</verification>

<success_criteria>
- Environment fully configured for prodex integration
- Executable resolution confirmed
- Fail-closed behavior preserved (missing VERSION/COMMIT = startup failure)
</success_criteria>

<output>
After completion, create `.planning/phases/00-fundacao/00-02-SUMMARY.md`
</output>
