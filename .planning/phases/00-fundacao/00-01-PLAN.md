---
phase: 00-fundacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [REQ-01]
user_setup: []
must_haves:
  truths:
    - "prodex source exists at ~/runtime/prodex-src on fleet host"
    - "git rev-parse HEAD starts with 7750da9b"
    - "target/release/prodex binary exists and is executable"
    - "prodex --version outputs v0.246.0"
    - "sha256sum of binary is recorded in evidence"
  artifacts:
    - path: "~/runtime/prodex-src/target/release/prodex"
      provides: "prodex runtime binary"
    - path: ".deploy-control/evidence/p0-binary-attestation.md"
      provides: "Binary hash and version attestation"
  key_links: []
---

<objective>
Stabilize prodex source and build the pinned binary via Docker container.

Purpose: The prodex binary is the SEV-0 blocker — nothing can proceed without it. Source is in ephemeral /tmp and must be moved to a stable location. Binary must be built from pinned commit 7750da9b (v0.246.0).
Output: Verified prodex binary at ~/runtime/prodex-src/target/release/prodex with hash attestation.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Diligencias/00_FUNDACAO_P0.md
@Diligencias/00b_DEPENDENCY_SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize prodex source out of /tmp</name>
  <files>~/runtime/prodex-src/</files>
  <read_first>Diligencias/00b_DEPENDENCY_SOURCES.md, Diligencias/00_FUNDACAO_P0.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27), NOT this orchestration host:

    1. Create stable directory:
       mkdir -p "$HOME/runtime"

    2. Copy source from /tmp to stable location:
       cp -a /tmp/prodex-audit-7750da9 "$HOME/runtime/prodex-src"

    3. If /tmp source is gone, re-clone:
       git clone https://github.com/christiandoxa/prodex "$HOME/runtime/prodex-src"
       git -C "$HOME/runtime/prodex-src" checkout 7750da9b6a5c91a6d429e18e6a4d422cab4bc144

    4. Verify pin:
       git -C "$HOME/runtime/prodex-src" rev-parse HEAD
       # MUST start with 7750da9b
  </action>
  <verify>git -C ~/runtime/prodex-src rev-parse HEAD | grep -q "^7750da9b"</verify>
  <acceptance_criteria>
    - "~/runtime/prodex-src exists and is a git repo"
    - "HEAD commit starts with 7750da9b6a5c"
    - "Cargo.toml contains name = \"prodex\""
  </acceptance_criteria>
  <done>Source stabilized at ~/runtime/prodex-src on correct commit</done>
</task>

<task type="auto">
  <name>Task 2: Build prodex binary via Docker container (IPv6 OFF)</name>
  <files>~/runtime/prodex-src/target/release/prodex</files>
  <read_first>Diligencias/00b_DEPENDENCY_SOURCES.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    Build in pinned Rust container. The Cargo.lock requires rustc >= 1.88.
    CI uses stable (currently 1.96.1). Use rust:1-bookworm (stable) instead of
    the originally documented rust:1.85-bookworm which is INSUFFICIENT.

    docker run --rm --sysctl net.ipv6.conf.all.disable_ipv6=1 \
      -v "$HOME/runtime/prodex-src":/src \
      -v prodex-cargo:/usr/local/cargo/registry \
      -w /src rust:1-bookworm bash -lc '
        set -e
        echo "Rust version: $(rustc --version)"
        cargo build --release 2>&amp;1 | tail -30
        ls -la target/release/prodex
      '

    If build fails due to missing system deps (openssl/pkg-config/cmake/protobuf):
    - Add apt-get install to the docker run command
    - CAPTURE the exact error and ESCALATE — do NOT invent flags

    After build, verify binary exists:
    ls -la "$HOME/runtime/prodex-src/target/release/prodex"
  </action>
  <verify>test -x ~/runtime/prodex-src/target/release/prodex</verify>
  <acceptance_criteria>
    - "target/release/prodex exists and is executable"
    - "Build completed with exit code 0"
    - "Rust version used is >= 1.85 (edition 2024 requirement)"
  </acceptance_criteria>
  <done>prodex binary built successfully from pinned source</done>
</task>

<task type="auto">
  <name>Task 3: Verify pin + Record integrity attestation</name>
  <files>.deploy-control/evidence/p0-binary-attestation.md</files>
  <read_first>Diligencias/00_FUNDACAO_P0.md</read_first>
  <action>
    On FLEET HOST (192.168.1.27):

    1. Verify version:
       BIN="$HOME/runtime/prodex-src/target/release/prodex"
       "$BIN" --version
       # Must show v0.246.0

    2. Record hash:
       sha256sum "$BIN"

    3. Create attestation evidence:
       mkdir -p .deploy-control/evidence
       cat > .deploy-control/evidence/p0-binary-attestation.md << 'EOF'
       # P0 Binary Attestation
       - Source: github.com/christiandoxa/prodex
       - Commit: 7750da9b6a5c91a6d429e18e6a4d422cab4bc144
       - Version: v0.246.0
       - Binary: ~/runtime/prodex-src/target/release/prodex
       - SHA256: [INSERT HASH]
       - Built: [INSERT TIMESTAMP]
       - Rust: [INSERT RUSTC VERSION]
       - Container: rust:1-bookworm
       EOF
  </action>
  <verify>"$HOME/runtime/prodex-src/target/release/prodex" --version | grep -q "v0.246.0"</verify>
  <acceptance_criteria>
    - "prodex --version outputs v0.246.0"
    - "sha256sum hash is recorded in evidence file"
    - ".deploy-control/evidence/p0-binary-attestation.md exists"
  </acceptance_criteria>
  <done>Version v0.246.0 confirmed, hash recorded as attestation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Source at ~/runtime/prodex-src (not /tmp)
- [ ] git rev-parse HEAD starts with 7750da9b
- [ ] target/release/prodex is executable
- [ ] prodex --version = v0.246.0
- [ ] sha256sum recorded in .deploy-control/evidence/
</verification>

<success_criteria>
- All 3 tasks completed on fleet host
- Binary pinned to correct version and commit
- Hash attestation recorded
- No secrets in evidence files
</success_criteria>

<output>
After completion, create `.planning/phases/00-fundacao/00-01-SUMMARY.md`
</output>
