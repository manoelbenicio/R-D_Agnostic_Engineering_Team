agent: GLM-5.2-Cline#1 (Cline)
stream: GLM52-CLINE1
phase: P0-CREDENTIAL-ISOLATION
task: Estender runtime_isolation_test.go para cobrir 6 vendors (Codex, Kiro, Antigravity, GLM, Cline, OpenCode) — 3 casos cada
priority: P0
status: DONE
progress: 100
started_at: 2026-07-06T21:21:28Z
finished_at: 2026-07-06T21:30:34Z
depends_on: none
blockers: none
files_owned:
  - multica-auth-work/server/internal/daemon/runtime_isolation_test.go
build_result: |
  GREEN — go test -v -count=1 -run TestCredentialIsolationPerVendor ./internal/daemon/
  18/18 subtests PASS (6 vendors × 3 casos). 0 FAIL. 0 SECRET- markers in evidence.
  Exit code 0. Duration 0.075s (fresh, no cache).
  Existing 6 runtime-isolation tests also PASS — no regression.
evidence: .deploy-control/evidence/GLM52CLINE1_test_evidence.txt
notes: |
  NÃO COMMITADO — git diff --cached vazio (staging area limpa). Apenas working-tree modification no arquivo próprio.

  == O QUE FOI FEITO ==
  Estendido runtime_isolation_test.go (+500 linhas) com TestCredentialIsolationPerVendor:
  tabela-driven sobre 6 vendors, 3 subtestes cada (18 total).

  == TABELAS REAIS DO POSTGRESQL (migration 123) ==
  Usa accounts, credentials, assignments, rotation_events — NUNCA rotation_accounts/credentials/assignments.
  Helper isolationEnsureSchema aplica 123_rotation.up.sql se a tabela accounts não existir.
  Helper isolationSeedAccount insere em accounts + credentials (secret_ref = ponteiro KMS, nunca plaintext).
  Helper isolationAssign insere em assignments (agent_id → account_id).
  Helper isolationCleanup remove apenas rows do tenant/agent do teste (não TRUNCATE global).

  == OS 3 CASOS POR VENDOR ==
  (1) two_accounts_coexist_without_overlap:
      - Cria 2 contas do mesmo vendor no PG com home_dirs diferentes + credenciais diferentes (marcadores únicos).
      - Assign agentA→accountA, agentB→accountB na tabela assignments.
      - credentialAccountHomeForTask resolve cada agent para sua própria home dir (diferentes, sem sobreposição).
      - execenv.Prepare cria 2 envs isolados; verifica dirs não se sobrepõem.
      - Lê credenciais em disco: A contém marcador-A (não B); B contém marcador-B (não A). Sem cross-contamination.

  (2) fail_closed_no_assignment:
      - Agent sem assignment na tabela assignments → credentialAccountHomeForTask retorna ERRO (não "").
      - Verifica home == "" (não credencial compartilhada) e err != nil.
      - Verifica mensagem de erro contém "no account assignment".
      - Store nil → também fail-closed (erro, não fallback).

  (3) no_secret_in_log:
      - Captura TODO log do gate (credentialAccountHomeForTask) e do prepare (execenv.Prepare) em buffer.
      - Confirma marcador está em disco (não é falso-negativo de fixture ausente).
      - Verifica marcador NÃO aparece em nenhum buffer de log.

  == ENV POR VENDOR (mapeado via execenv.Environment) ==
  Codex       → CODEX_HOME            (env.CodexHome, auth.json)
  Kiro        → XDG_DATA_HOME         (env.KiroDataHome, kiro-cli/data.sqlite3)
  Antigravity → HOME                  (env.AntigravityHome, .gemini/antigravity-cli/)
  GLM         → XDG_DATA+CONFIG_HOME  (env.OpenCodeDataHome/ConfigHome, .local/share/opencode/)
  Cline       → CLINE_DATA_DIR        (env.ClineDataDir, .cline/)
  OpenCode    → XDG_DATA+CONFIG_HOME  (env.OpenCodeDataHome/ConfigHome, .local/share/opencode/)

  == EVIDÊNCIA DE GREEN ==
  $ DATABASE_URL=postgres://multica:multica@172.21.0.2:5432/multica?sslmode=disable \
    go test -v -count=1 -timeout 120s -run TestCredentialIsolationPerVendor ./internal/daemon/
  --- PASS: TestCredentialIsolationPerVendor (0.04s)
      --- PASS: TestCredentialIsolationPerVendor/codex (0.05s)
          --- PASS: .../codex/two_accounts_coexist_without_overlap (0.03s)
          --- PASS: .../codex/fail_closed_no_assignment (0.00s)
          --- PASS: .../codex/no_secret_in_log (0.02s)
      --- PASS: TestCredentialIsolationPerVendor/kiro (0.05s)
          [3/3 PASS]
      --- PASS: TestCredentialIsolationPerVendor/antigravity (0.06s)
          [3/3 PASS]
      --- PASS: TestCredentialIsolationPerVendor/cline (0.06s)
          [3/3 PASS]
      --- PASS: TestCredentialIsolationPerVendor/opencode (0.06s)
          [3/3 PASS]
      --- PASS: TestCredentialIsolationPerVendor/glm (0.06s)
          [3/3 PASS]
  PASS
  ok  github.com/multica-ai/multica/server/internal/daemon  0.075s

  Sem segredo em log: 0 ocorrências de "SECRET-" no arquivo de evidência.
