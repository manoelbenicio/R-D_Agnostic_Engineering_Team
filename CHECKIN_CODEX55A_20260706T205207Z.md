# CHECKIN CODEX55A 20260706T205207Z

## Escopo
- Fatia CODEX-A: `daemon.go` e `execenv/codex_home.go`.
- Objetivo: fail-closed para credenciais por conta e remoção de symlink de `auth.json`.

## Estado inicial
- Documentos lidos integralmente:
  - `docs/operations/FIX_ISOLAMENTO_CREDENCIAL_CENTRAL.md`
  - `docs/operations/CREDENTIAL_ISOLATION_REFERENCES.md`
- Referência AOP lida:
  - `/mnt/c/VMs/Projects/AOP/control-plane/sessions_api/service.py`
- Observação: o primeiro check-in foi criado antes das edições, mas por erro no diretório inicial da sessão; este arquivo replica o check-in no path obrigatório do repo RD e registra a evidência final.

## Implementado
- `daemon.go`: `credentialAccountHomeForTask` agora retorna `(string, error)` e falha fechado para providers de isolamento (`codex`, `kiro`, `antigravity`, `glm`, `cline`, `opencode`) quando não há rotation store, agent id, atribuição, conta, vendor compatível ou `home_dir`.
- `daemon.go`: `custom_env` agora bloqueia override de `XDG_DATA_HOME`, preservando a env isolada do Kiro.
- `codex_home.go`: removido fallback de symlink para `auth.json`; a preparação agora sempre copia `auth.json` do `AccountHome` ou do shared home.
- `execenv_test.go`: `TestPrepareCodexHomeSeedsFromShared` atualizado, com autorização do TL, para exigir cópia regular de `auth.json` em vez de symlink.

## Evidência
- Formatação:
  - `docker run --rm -v "$PWD/multica-auth-work/server:/src" -w /src golang:1.26-alpine gofmt -w internal/daemon/daemon.go internal/daemon/execenv/codex_home.go internal/daemon/execenv/execenv_test.go` — passou.
  - `git diff --check -- multica-auth-work/server/internal/daemon/daemon.go multica-auth-work/server/internal/daemon/execenv/codex_home.go multica-auth-work/server/internal/daemon/execenv/execenv_test.go .deploy-control/ASK_KIRO.md CHECKIN_CODEX55A_20260706T205207Z.md` — passou.
- Testes:
  - `docker run --rm -v "$PWD/multica-auth-work/server:/src" -w /src golang:1.26-alpine go test ./internal/daemon/execenv` — passou.
  - `docker run --rm -v "$PWD/multica-auth-work/server:/src" -w /src golang:1.26-alpine sh -c 'apk add --no-cache git >/tmp/apk.log && /usr/local/go/bin/go test ./internal/daemon'` — passou.

## Bloqueio resolvido
- Bloqueio registrado em `.deploy-control/ASK_KIRO.md` em `2026-07-06T20:55:49Z`.
- TL autorizou editar `execenv_test.go` para corrigir `TestPrepareCodexHomeSeedsFromShared`; teste atualizado e validação focada verde.
